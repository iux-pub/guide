---
phase: 06-rollout-handoff-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - starter/package.json
  - starter/.stylelintrc.json
  - starter/src/scss/style.scss
  - starter/src/scss/_project-overrides.scss
  - starter/src/scss/1-settings/_index.scss
  - starter/src/scss/1-settings/_tokens-color.scss
  - starter/src/scss/1-settings/_tokens-typography.scss
  - starter/src/scss/1-settings/_tokens-spacing.scss
  - starter/src/scss/1-settings/_tokens-grid.scss
  - starter/src/scss/1-settings/_tokens-misc.scss
  - starter/src/scss/1-settings/_breakpoints.scss
  - starter/src/scss/2-tools/_index.scss
  - starter/src/scss/2-tools/_responsive.scss
  - starter/src/scss/2-tools/_mixins.scss
  - starter/src/scss/2-tools/_functions.scss
  - starter/src/scss/3-generic/_index.scss
  - starter/src/scss/3-generic/_normalize.scss
  - starter/src/scss/3-generic/_box-sizing.scss
  - starter/src/scss/4-elements/_index.scss
  - starter/src/scss/4-elements/_base.scss
  - starter/src/scss/4-elements/_headings.scss
  - starter/src/scss/4-elements/_links.scss
  - starter/src/scss/5-objects/_index.scss
  - starter/src/scss/5-objects/_container.scss
  - starter/src/scss/5-objects/_grid.scss
  - starter/src/scss/6-components/_index.scss
  - starter/src/scss/6-components/_btn.scss
  - starter/src/scss/6-components/_form.scss
  - starter/src/scss/6-components/_card.scss
  - starter/src/scss/6-components/_table.scss
  - starter/src/scss/6-components/_modal.scss
  - starter/src/scss/6-components/_tab.scss
  - starter/src/scss/6-components/_pagination.scss
  - starter/src/scss/6-components/_breadcrumb.scss
  - starter/src/scss/7-utilities/_index.scss
  - starter/src/scss/7-utilities/_sr-only.scss
  - starter/src/scss/7-utilities/_visibility.scss
  - starter/src/js/modal.js
  - starter/src/js/tab.js
  - starter/index.html
  - starter/README.md
autonomous: true
requirements:
  - HAND-03

must_haves:
  truths:
    - "starter/ 디렉토리를 복사한 후 npm install && npm run build:css만으로 첫 빌드가 성공한다"
    - "스타터 킷에 ITCSS 전체 구조, 토큰, 믹스인, normalize, 컴포넌트가 모두 포함된다"
    - "_project-overrides.scss에 Primary 색상 변경 가이드 주석이 있다"
  artifacts:
    - path: "starter/package.json"
      provides: "스타터 킷 빌드 스크립트"
      contains: "build:css"
    - path: "starter/src/scss/style.scss"
      provides: "ITCSS 메인 진입점"
      contains: "@use"
    - path: "starter/_project-overrides.scss"
      provides: "프로젝트별 오버라이드 가이드"
      contains: "--color-primary"
    - path: "starter/index.html"
      provides: "HTML 보일러플레이트"
      contains: "skip-to-content"
    - path: "starter/README.md"
      provides: "스타터 킷 사용 안내"
      contains: "npm install"
  key_links:
    - from: "starter/package.json"
      to: "starter/src/scss/style.scss"
      via: "build:css 스크립트"
      pattern: "sass.*style.scss"
    - from: "starter/src/scss/style.scss"
      to: "starter/src/scss/1-settings/ ~ 7-utilities/"
      via: "@use로 각 레이어 로드"
      pattern: "@use"
---

<objective>
프로젝트 스타터 킷을 starter/ 디렉토리에 구성한다. 기존 src/scss/ 파일에서 추출하여 clone 후 바로 시작 가능한 최소 파일셋을 제공한다.

Purpose: 새 프로젝트를 시작할 때 SCSS 아키텍처, 토큰, 믹스인, 컴포넌트가 즉시 준비된 상태로 개발을 시작할 수 있다.
Output: starter/ 디렉토리에 완전한 ITCSS 구조 + package.json + HTML 보일러플레이트 + README
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rollout-handoff-onboarding/06-CONTEXT.md

@package.json
@src/scss/style.scss
@src/scss/_project-overrides.scss
@src/snippets/boilerplate.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 스타터 킷 SCSS 구조 + JS + HTML 보일러플레이트 복사</name>
  <files>starter/src/scss/ (전체 ITCSS 구조), starter/src/js/modal.js, starter/src/js/tab.js, starter/index.html</files>
  <read_first>src/scss/style.scss, src/scss/_project-overrides.scss, src/scss/1-settings/_index.scss, src/scss/1-settings/_tokens-color.scss, src/scss/2-tools/_index.scss, src/scss/2-tools/_responsive.scss, src/scss/2-tools/_mixins.scss, src/scss/3-generic/_index.scss, src/scss/4-elements/_index.scss, src/scss/5-objects/_index.scss, src/scss/6-components/_index.scss, src/scss/7-utilities/_index.scss, src/js/modal.js, src/js/tab.js, src/snippets/boilerplate.md</read_first>
  <action>
기존 src/scss/ 파일들을 starter/src/scss/로 복사한다. 파일 내용은 원본과 동일하게 유지하되 다음 조정을 적용한다:

1. starter/src/scss/ 전체 ITCSS 구조 복사 (per D-08):
   - style.scss (메인 진입점)
   - _project-overrides.scss (per D-09: Primary 색상 변경 가이드 주석 강화)
   - 1-settings/ 전체 (토큰 6개 파일 + _breakpoints.scss + _index.scss)
   - 2-tools/ 전체 (_responsive.scss, _mixins.scss, _functions.scss, _index.scss)
   - 3-generic/ 전체 (_normalize.scss, _box-sizing.scss, _index.scss)
   - 4-elements/ 전체 (_base.scss, _headings.scss, _links.scss, _index.scss)
   - 5-objects/ 전체 (_container.scss, _grid.scss, _index.scss)
   - 6-components/ 전체 (btn, form, card, table, modal, tab, pagination, breadcrumb + _index.scss)
   - 7-utilities/ 전체 (_sr-only.scss, _visibility.scss, _index.scss)

2. starter/src/scss/_project-overrides.scss 강화 (per D-09):
   기존 주석에 더 상세한 안내 추가:
   ```scss
   // ============================================================
   // 프로젝트별 토큰 오버라이드
   // ============================================================
   // 이 파일에서 프로젝트에 맞는 Primary 색상을 변경하세요.
   // 기본값: --color-primary: #256ef4 (파란색)
   //
   // 사용법:
   // 1. 아래 :root 블록의 주석을 해제하세요
   // 2. 프로젝트 브랜드 색상으로 변경하세요
   // 3. npm run build:css로 빌드하세요
   //
   // :root {
   //   --color-primary: #0a7b4f;        // 프로젝트 메인 색상
   //   --color-primary-light: #3da87a;  // 밝은 변형
   //   --color-primary-dark: #065535;   // 어두운 변형
   // }
   //
   // 추가 오버라이드 가능한 토큰:
   // --color-bg, --color-text, --font-family-base 등
   // 전체 토큰 목록: src/scss/1-settings/_tokens-*.scss 참조
   ```

3. starter/src/js/ 복사 (per D-08: 모달/탭만 포함):
   - modal.js — src/js/modal.js 그대로 복사
   - tab.js — src/js/tab.js 그대로 복사

4. starter/index.html 생성:
   src/snippets/boilerplate.md의 HTML 마크업을 기반으로 실제 파일 생성. CSS 경로를 `dist/css/style.css`로, JS 경로를 `src/js/modal.js`, `src/js/tab.js`로 설정. title을 "새 프로젝트"로 설정.

**주의:** docs.scss는 문서 사이트 전용이므로 스타터 킷에 포함하지 않는다. playground/ 디렉토리도 제외한다.
  </action>
  <verify>
    <automated>cd "/Users/johyeonchang/Documents/Work/프로젝트/infoUX" && test -f starter/src/scss/style.scss && test -f starter/src/scss/_project-overrides.scss && test -d starter/src/scss/1-settings && test -d starter/src/scss/6-components && test -f starter/src/js/modal.js && test -f starter/src/js/tab.js && test -f starter/index.html && grep -q "@use" starter/src/scss/style.scss && grep -q "color-primary" starter/src/scss/_project-overrides.scss && grep -q "skip-to-content" starter/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - starter/src/scss/style.scss가 존재하고 @use로 7개 레이어를 로드한다
    - starter/src/scss/1-settings/ ~ 7-utilities/ 7개 디렉토리가 모두 존재한다
    - starter/src/scss/6-components/에 8개 컴포넌트 SCSS 파일이 있다
    - starter/src/scss/_project-overrides.scss에 --color-primary 변경 가이드 주석이 있다
    - starter/src/js/modal.js와 tab.js가 존재한다
    - starter/index.html에 skip-to-content, lang="ko", main-content가 포함된다
    - docs.scss가 starter/에 포함되지 않는다
  </acceptance_criteria>
  <done>스타터 킷에 ITCSS 전체 구조, 토큰, 믹스인, 컴포넌트, JS, HTML 보일러플레이트가 복사되었다</done>
</task>

<task type="auto">
  <name>Task 2: 스타터 킷 package.json + .stylelintrc.json + README.md 생성</name>
  <files>starter/package.json, starter/.stylelintrc.json, starter/README.md</files>
  <read_first>package.json, src/scss/style.scss, .planning/phases/06-rollout-handoff-onboarding/06-CONTEXT.md</read_first>
  <action>
1. starter/package.json 생성 (per D-10):
   기존 package.json에서 스타터 킷에 필요한 최소 항목만 추출:
   ```json
   {
     "name": "infomind-project-starter",
     "version": "1.0.0",
     "description": "인포마인드 UX 프로젝트 스타터 킷 — ITCSS + BEM + 디자인 토큰",
     "private": true,
     "scripts": {
       "build:css": "sass src/scss/style.scss dist/css/style.css --load-path=node_modules",
       "watch:css": "sass src/scss/style.scss dist/css/style.css --load-path=node_modules --watch",
       "lint:css": "stylelint \"src/scss/**/*.scss\"",
       "lint:css:fix": "stylelint \"src/scss/**/*.scss\" --fix"
     },
     "dependencies": {
       "modern-normalize": "^3.0.1",
       "sass": "^1.98.0",
       "sass-rem": "^4.0.1"
     },
     "devDependencies": {
       "stylelint": "^17.5.0",
       "stylelint-config-standard-scss": "^17.0.0",
       "stylelint-selector-bem-pattern": "^4.0.1"
     }
   }
   ```
   - 문서 사이트 관련 패키지 제외: @11ty/eleventy, eleventy-plugin-syntaxhighlight, pagefind, concurrently, clipboard, pa11y-ci, @axe-core/cli
   - build:css와 lint:css만 포함 (per D-10: npm install && npm run build:css로 첫 빌드)

2. starter/.stylelintrc.json 생성:
   프로젝트 루트의 .stylelintrc.json 내용을 그대로 복사한다. 단, 루트에 파일이 없는 경우(git untracked 상태) Phase 2에서 만든 설정을 CLAUDE.md의 린트 규칙 설명을 참고하여 재현:
   ```json
   {
     "extends": ["stylelint-config-standard-scss"],
     "plugins": ["stylelint-selector-bem-pattern"],
     "defaultSeverity": "warning",
     "rules": {
       "selector-class-pattern": null,
       "scss/selector-class-pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*(__[a-z0-9]+(-[a-z0-9]+)*)?(--[a-z0-9]+(-[a-z0-9]+)*)?$"
     }
   }
   ```
   실제 프로젝트의 .stylelintrc.json을 먼저 읽어서 정확한 내용을 복사하라. 파일이 없으면 위 최소 설정 사용.

3. starter/README.md 생성:
   ```markdown
   # 인포마인드 프로젝트 스타터 킷

   ITCSS + BEM + 디자인 토큰 기반 SCSS 프로젝트 시작 템플릿

   ## 시작하기

   1. 이 디렉토리를 프로젝트 폴더로 복사
   2. npm install
   3. npm run build:css

   dist/css/style.css가 생성되면 성공!

   ## 프로젝트 커스터마이징

   ### Primary 색상 변경
   src/scss/_project-overrides.scss 파일에서 주석을 해제하고 브랜드 색상으로 변경

   ### 새 컴포넌트 추가
   1. src/scss/6-components/_컴포넌트명.scss 생성
   2. src/scss/6-components/_index.scss에 @forward 추가

   ## 사용 가능한 스크립트

   | 명령어 | 설명 |
   |--------|------|
   | npm run build:css | SCSS 빌드 |
   | npm run watch:css | SCSS 변경 감지 자동 빌드 |
   | npm run lint:css | Stylelint 검사 |
   | npm run lint:css:fix | Stylelint 자동 수정 |

   ## 포함된 컴포넌트

   버튼, 폼, 카드, 테이블, 모달, 탭, 페이지네이션, 브레드크럼

   ## 참고 문서

   전체 가이드: [인포마인드 UX 가이드 사이트] 참조
   ```
  </action>
  <verify>
    <automated>cd "/Users/johyeonchang/Documents/Work/프로젝트/infoUX" && test -f starter/package.json && test -f starter/README.md && node -e "const p=JSON.parse(require('fs').readFileSync('starter/package.json','utf8')); if(!p.scripts['build:css']||!p.dependencies.sass) process.exit(1); if(p.devDependencies['@11ty/eleventy']) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - starter/package.json이 존재하고 build:css, watch:css, lint:css 스크립트가 있다
    - starter/package.json에 sass, modern-normalize, sass-rem이 dependencies에 있다
    - starter/package.json에 @11ty/eleventy, pa11y-ci 등 문서 사이트 전용 패키지가 없다
    - starter/.stylelintrc.json이 존재하고 BEM 패턴 규칙이 포함된다
    - starter/README.md가 존재하고 "npm install", "build:css" 안내가 있다
  </acceptance_criteria>
  <done>스타터 킷이 독립적으로 npm install && npm run build:css를 실행할 수 있는 완전한 패키지이다</done>
</task>

</tasks>

<verification>
- starter/ 디렉토리에서 독립적으로 `npm install && npm run build:css`가 성공하는지 확인
- 생성된 dist/css/style.css에 토큰(CSS Custom Properties)이 포함되는지 확인
- .stylelintrc.json으로 `npm run lint:css` 실행 가능한지 확인
</verification>

<success_criteria>
- starter/ 디렉토리에 ITCSS 7레이어 전체 구조가 있다
- package.json에 최소 필요 의존성만 포함된다
- npm install && npm run build:css로 첫 빌드가 성공한다
- _project-overrides.scss에 Primary 색상 변경 가이드가 주석으로 있다
- README.md에 시작 방법이 안내되어 있다
- HTML 보일러플레이트(index.html)에 접근성 필수 요소가 포함된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-rollout-handoff-onboarding/06-02-SUMMARY.md`
</output>
