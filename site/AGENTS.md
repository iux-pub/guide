# Documentation Scope

1. 문서 예시도 제품 코드와 같은 토큰·시맨틱·접근성 규칙을 따른다.
2. 의도적으로 금지 패턴을 보여줄 때는 코드 블록과 설명에서 명확히 `금지 예시`로 표시한다.
3. 문서 페이지의 정보 구조와 링크를 깨지 않는다.
4. 자동 생성 페이지는 원본 데이터나 빌더를 수정한다.
5. 새 governance 문서는 결정, 적용 범위, 실행 순서, 검증 방법을 포함한다.
