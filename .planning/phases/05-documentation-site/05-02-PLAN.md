---
phase: 05-documentation-site
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - site/tokens/tokens.json
  - site/tokens/index.md
  - site/tokens/color.md
  - site/tokens/typography.md
  - site/tokens/spacing.md
  - site/tokens/grid.md
  - site/conventions/conventions.json
  - site/conventions/index.md
  - site/conventions/bem.md
  - site/conventions/scss-structure.md
  - site/accessibility/accessibility.json
  - site/accessibility/index.md
  - site/accessibility/checklist.md
  - site/accessibility/color-contrast.md
  - site/accessibility/sr-only.md
  - site/accessibility/btn.md
  - site/accessibility/form.md
  - site/accessibility/card.md
  - site/accessibility/table.md
  - site/accessibility/modal.md
  - site/accessibility/tab.md
  - site/accessibility/breadcrumb.md
  - site/accessibility/pagination.md
autonomous: true
requirements:
  - DOCS-05
  - AI-01

must_haves:
  truths:
    - "빌드 후 _site/tokens/ 디렉토리에 5개 HTML 파일 존재 (index, color, typography, spacing, grid)"
    - "빌드 후 _site/conventions/ 디렉토리에 3개 HTML 파일 존재 (index, bem, scss-structure)"
    - "빌드 후 _site/accessibility/ 디렉토리에 13개 HTML 파일 존재"
    - "각 페이지에 사이드바 네비게이션이 렌더링된다"
    - "CLAUDE.md에 접근성 가이드 경로(docs/accessibility/) 안내가 포함된다"
  artifacts:
    - path: "site/tokens/color.md"
      provides: "색상 토큰 문서 페이지"
      contains: "color-primary"
    - path: "site/conventions/bem.md"
      provides: "BEM 가이드 문서 페이지"
      contains: "Block__Element--Modifier"
    - path: "site/accessibility/checklist.md"
      provides: "KWCAG 체크리스트 페이지"
      contains: "KWCAG"
    - path: "CLAUDE.md"
      provides: "AI 지시문 업데이트"
      contains: "docs/accessibility"
  key_links:
    - from: "site/tokens/*.md"
      to: "site/_includes/layouts/page.njk"
      via: "front matter layout"
      pattern: "layout.*layouts/page"
    - from: "site/accessibility/*.md"
      to: "site/_includes/layouts/page.njk"
      via: "front matter layout"
      pattern: "layout.*layouts/page"
---

<objective>
토큰, 컨벤션, 접근성 3개 섹션의 모든 가이드 문서 페이지를 마크다운으로 생성한다. 기존 docs/, src/scss/1-settings/ 파일의 콘텐츠를 site/ 디렉토리에 문서 사이트용으로 재구성하고, CLAUDE.md에 접근성 가이드 경로를 추가한다.

Purpose: 기존 Phase 1~4에서 만든 가이드 콘텐츠를 Eleventy 문서 사이트로 통합하여 한곳에서 열람 가능하게 한다.
Output: 21개 마크다운 문서 페이지 + CLAUDE.md 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-site/05-CONTEXT.md
@.planning/phases/05-documentation-site/05-01-SUMMARY.md

@src/scss/1-settings/_tokens-color.scss
@src/scss/1-settings/_tokens-typography.scss
@src/scss/1-settings/_tokens-spacing.scss
@src/scss/1-settings/_tokens-grid.scss
@src/scss/1-settings/_tokens-misc.scss
@docs/bem-guide.md
@docs/scss-structure-guide.md
@docs/accessibility/checklist.md
@docs/accessibility/color-contrast.md
@docs/accessibility/sr-only.md
@docs/accessibility/btn.md
@docs/accessibility/form.md
@docs/accessibility/card.md
@docs/accessibility/table.md
@docs/accessibility/modal.md
@docs/accessibility/tab.md
@docs/accessibility/breadcrumb.md
@docs/accessibility/pagination.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 토큰 + 컨벤션 섹션 마크다운 페이지 생성</name>
  <files>
    site/tokens/tokens.json,
    site/tokens/index.md,
    site/tokens/color.md,
    site/tokens/typography.md,
    site/tokens/spacing.md,
    site/tokens/grid.md,
    site/conventions/conventions.json,
    site/conventions/index.md,
    site/conventions/bem.md,
    site/conventions/scss-structure.md
  </files>
  <read_first>
    src/scss/1-settings/_tokens-color.scss,
    src/scss/1-settings/_tokens-typography.scss,
    src/scss/1-settings/_tokens-spacing.scss,
    src/scss/1-settings/_tokens-grid.scss,
    src/scss/1-settings/_tokens-misc.scss,
    docs/bem-guide.md,
    docs/scss-structure-guide.md,
    site/_data/navigation.json (Plan 01에서 생성)
  </read_first>
  <action>
    1. 디렉토리 데이터 파일 생성 (per RESEARCH Pattern 3):
       - site/tokens/tokens.json: `{ "layout": "layouts/page.njk", "section": "tokens", "tags": "token" }`
       - site/conventions/conventions.json: `{ "layout": "layouts/page.njk", "section": "conventions", "tags": "convention" }`

    2. 토큰 섹션 마크다운 (5개):
       - **index.md** — 토큰 개요: 토큰이란 무엇인지, 사용 원칙(하드코딩 금지), 토큰 카테고리 안내
       - **color.md** — 색상 토큰: _tokens-color.scss의 모든 변수명과 값을 문서화. Primary, Gray scale, Semantic, Text, Background, Border 카테고리별 정리. 사용 예시 코드 블록 포함
       - **typography.md** — 타이포그래피 토큰: font-family, font-size, font-weight, line-height. 62.5% REM 트릭 설명. 사용 예시
       - **spacing.md** — 간격 토큰: 4px 기반 스케일(xs~3xl). 사용 예시
       - **grid.md** — 그리드 토큰: 12컬럼, 거터, 컨테이너 max-width, 반응형 브레이크포인트. container/grid 오브젝트 사용법

       각 페이지 front matter: `title`, `order` (사이드바 정렬용)
       콘텐츠: SCSS 파일에서 실제 변수명과 값을 정확히 추출. SCSS 코드 블록으로 예시 포함. 한국어 작성

    3. 컨벤션 섹션 마크다운 (3개):
       - **index.md** — 컨벤션 개요: BEM과 SCSS 구조 소개
       - **bem.md** — docs/bem-guide.md 콘텐츠 기반으로 front matter 추가 + 문서 사이트 형식으로 재구성. BEM 패턴, do/don't, SCSS 중첩 규칙 포함
       - **scss-structure.md** — docs/scss-structure-guide.md 콘텐츠 기반으로 front matter 추가 + 재구성. ITCSS 7레이어, 파일 배치 규칙, 새 파일 추가 방법
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run build:site 2>&1 | tail -3 && ls _site/tokens/ | wc -l && ls _site/conventions/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - _site/tokens/ 디렉토리에 index.html, color/index.html, typography/index.html, spacing/index.html, grid/index.html 존재
    - _site/conventions/ 디렉토리에 index.html, bem/index.html, scss-structure/index.html 존재
    - site/tokens/color.md에 `--color-primary` 포함
    - site/conventions/bem.md에 `Block__Element--Modifier` 포함
  </acceptance_criteria>
  <done>토큰 5개 + 컨벤션 3개 페이지가 빌드되어 _site/에 HTML로 출력됨</done>
</task>

<task type="auto">
  <name>Task 2: 접근성 섹션 마크다운 페이지 + CLAUDE.md 업데이트 (per D-07, AI-01)</name>
  <files>
    site/accessibility/accessibility.json,
    site/accessibility/index.md,
    site/accessibility/checklist.md,
    site/accessibility/color-contrast.md,
    site/accessibility/sr-only.md,
    site/accessibility/btn.md,
    site/accessibility/form.md,
    site/accessibility/card.md,
    site/accessibility/table.md,
    site/accessibility/modal.md,
    site/accessibility/tab.md,
    site/accessibility/breadcrumb.md,
    site/accessibility/pagination.md,
    CLAUDE.md
  </files>
  <read_first>
    docs/accessibility/checklist.md,
    docs/accessibility/color-contrast.md,
    docs/accessibility/sr-only.md,
    docs/accessibility/btn.md,
    docs/accessibility/form.md,
    docs/accessibility/card.md,
    docs/accessibility/table.md,
    docs/accessibility/modal.md,
    docs/accessibility/tab.md,
    docs/accessibility/breadcrumb.md,
    docs/accessibility/pagination.md,
    CLAUDE.md
  </read_first>
  <action>
    1. site/accessibility/accessibility.json 생성:
       `{ "layout": "layouts/page.njk", "section": "accessibility", "tags": "accessibility" }`

    2. 접근성 섹션 마크다운 (13개):
       - **index.md** — 접근성 개요: KWCAG/WCAG 2.1 AA 기준, 접근성 원칙, 컴포넌트별 가이드 목록
       - **checklist.md ~ pagination.md** — docs/accessibility/ 의 각 마크다운 파일을 site/accessibility/로 복사하되:
         - YAML front matter 추가: title, order, section
         - 콘텐츠는 원본과 동일하게 유지 (복사 후 front matter만 추가)
         - 원본 docs/accessibility/는 그대로 보존 (AI 직접 참조용)

    3. CLAUDE.md 업데이트 (per D-07, AI-01 요구사항):
       - 기존 CLAUDE.md의 적절한 위치에 접근성 가이드 경로 안내 추가:
         ```
         ### 접근성 가이드 참조 경로
         - 퍼블리싱 체크리스트: docs/accessibility/checklist.md
         - 색상 대비 가이드: docs/accessibility/color-contrast.md
         - 스크린리더 전용 패턴: docs/accessibility/sr-only.md
         - 컴포넌트별 접근성: docs/accessibility/{component}.md (btn, form, card, table, modal, tab, breadcrumb, pagination)
         ```
       - 문서 사이트 경로 안내 추가: "문서 사이트: `npm run build:site`로 빌드 후 _site/ 에서 열람"
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run build:site 2>&1 | tail -3 && ls _site/accessibility/ | wc -l && grep -q "docs/accessibility" CLAUDE.md && echo "CLAUDE.md OK"</automated>
  </verify>
  <acceptance_criteria>
    - _site/accessibility/ 디렉토리에 13개 하위 디렉토리/파일 존재
    - site/accessibility/checklist.md에 `layout: layouts/page.njk` front matter 포함
    - CLAUDE.md에 `docs/accessibility/checklist.md` 경로 포함
    - CLAUDE.md에 `npm run build:site` 안내 포함
  </acceptance_criteria>
  <done>접근성 13개 페이지가 빌드되어 _site/에 HTML로 출력되고, CLAUDE.md에 접근성 가이드 경로가 포함되어 AI가 즉시 참조 가능</done>
</task>

</tasks>

<verification>
- `npm run build:site` 성공
- _site/tokens/에 5개 페이지 HTML 존재
- _site/conventions/에 3개 페이지 HTML 존재
- _site/accessibility/에 13개 페이지 HTML 존재
- 각 페이지에 사이드바 네비게이션 렌더링
- CLAUDE.md에 접근성 가이드 경로 포함
- `grep -q "docs/accessibility" CLAUDE.md` 성공
</verification>

<success_criteria>
토큰(5), 컨벤션(3), 접근성(13) 총 21개 가이드 문서 페이지가 Eleventy로 빌드되어 사이드바 네비게이션과 함께 열람 가능하고, CLAUDE.md에 모든 가이드 경로가 안내되어 AI 프롬프트로 즉시 활용 가능
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-site/05-02-SUMMARY.md`
</output>
