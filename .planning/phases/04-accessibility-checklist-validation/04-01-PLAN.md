---
phase: 04-accessibility-checklist-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .pa11yci.js
  - package.json
  - reports/.gitkeep
  - .gitignore
  - docs/accessibility/checklist.md
autonomous: true
requirements: [A11Y-01, A11Y-02, A11Y-06]
user_setup: []

must_haves:
  truths:
    - "npm run test:a11y를 실행하면 playground 9개 HTML이 WCAG2AA 기준으로 검사된다"
    - "KWCAG 2.2 33개 검사항목이 컴포넌트별로 분류된 마크다운 체크리스트가 존재한다"
    - "pa11y-ci 결과가 reports/a11y-report.json에 저장된다"
  artifacts:
    - path: ".pa11yci.js"
      provides: "pa11y-ci 설정 (playground 9개 URL, WCAG2AA 기준)"
      contains: "WCAG2AA"
    - path: "package.json"
      provides: "test:a11y npm script"
      contains: "test:a11y"
    - path: "reports/.gitkeep"
      provides: "리포트 출력 디렉토리"
    - path: "docs/accessibility/checklist.md"
      provides: "KWCAG 2.2 33개 항목 마크다운 체크리스트"
      min_lines: 100
  key_links:
    - from: "package.json"
      to: ".pa11yci.js"
      via: "test:a11y script"
      pattern: "pa11y-ci --config .pa11yci.js"
    - from: ".pa11yci.js"
      to: "src/playground/*.html"
      via: "file:// URL 목록"
      pattern: "file://"
---

<objective>
pa11y-ci 자동 접근성 테스트 파이프라인을 구축하고, KWCAG 2.2 33개 검사항목 기반 퍼블리싱 체크리스트를 마크다운 문서로 작성한다.

Purpose: 접근성 자동 검증 인프라 + 실무 체크리스트로 KWCAG/WCAG AA 준수를 체계적으로 보장
Output: .pa11yci.js, npm scripts, docs/accessibility/checklist.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accessibility-checklist-validation/04-CONTEXT.md
@.planning/phases/04-accessibility-checklist-validation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pa11y-ci 설치 + 설정 파일 + npm scripts 추가</name>
  <files>.pa11yci.js, package.json, reports/.gitkeep, .gitignore</files>
  <read_first>
    - package.json
    - .gitignore
    - src/playground/ (디렉토리 목록 확인)
  </read_first>
  <action>
1. pa11y-ci와 @axe-core/cli 설치 (per D-06):
   ```bash
   npm install --save-dev pa11y-ci@^4.1.0 @axe-core/cli@^4.11.1 --legacy-peer-deps
   ```

2. `.pa11yci.js` 생성 (per D-04, D-05). JavaScript 설정 파일로 절대 경로 동적 생성:
   ```javascript
   // pa11y-ci 설정 — playground HTML 접근성 검사
   const path = require('path')

   const playgroundDir = path.resolve(__dirname, 'src/playground')

   const pages = [
     'index.html',
     'btn.html',
     'form.html',
     'card.html',
     'table.html',
     'modal.html',
     'tab.html',
     'pagination.html',
     'breadcrumb.html'
   ]

   module.exports = {
     defaults: {
       standard: 'WCAG2AA',
       timeout: 30000,
       viewport: { width: 1280, height: 800 },
       reporters: [
         'cli',
         ['json', { fileName: 'reports/a11y-report.json' }]
       ],
       chromeLaunchConfig: {
         args: ['--no-sandbox']
       }
     },
     urls: pages.map(p => `file://${path.join(playgroundDir, p)}`)
   }
   ```
   주의: boilerplate.html은 포함하지 않음 (빈 보일러플레이트 골격이므로 검사 대상 아님). a11y-checklist.html은 Plan 03에서 추가.

3. package.json scripts에 추가:
   - `"test:a11y": "npm run build:css && pa11y-ci --config .pa11yci.js"` (CSS 빌드 선행 필수 — Pitfall 2 방지)
   - `"test:a11y:single": "axe"` (개별 페이지 빠른 검사용)

4. `reports/.gitkeep` 빈 파일 생성.

5. `.gitignore`에 `reports/a11y-report.json` 추가 (리포트는 git 제외).
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && node -e "const c = require('./.pa11yci.js'); console.log(c.defaults.standard, c.urls.length)" | grep "WCAG2AA 9" && grep "test:a11y" package.json && test -f reports/.gitkeep && grep "a11y-report" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - .pa11yci.js 파일이 존재하고 `standard: 'WCAG2AA'`와 9개 URL을 포함
    - package.json에 `"test:a11y"` 스크립트가 `pa11y-ci --config .pa11yci.js`를 실행
    - package.json에 `"test:a11y:single"` 스크립트가 `axe`를 실행
    - reports/.gitkeep 파일 존재
    - .gitignore에 `reports/a11y-report.json` 포함
    - pa11y-ci, @axe-core/cli가 devDependencies에 존재
  </acceptance_criteria>
  <done>npm run test:a11y가 playground 9개 HTML을 WCAG2AA로 검사하는 파이프라인이 동작하고, 결과가 터미널 + reports/a11y-report.json에 출력된다</done>
</task>

<task type="auto">
  <name>Task 2: KWCAG 2.2 퍼블리싱 체크리스트 마크다운 작성</name>
  <files>docs/accessibility/checklist.md</files>
  <read_first>
    - .planning/phases/04-accessibility-checklist-validation/04-RESEARCH.md (KWCAG 2.2 33개 항목 전체 목록)
    - src/snippets/btn.md (스니펫 접근성 섹션 구조 확인 — 중복 방지)
  </read_first>
  <action>
`docs/accessibility/` 디렉토리를 생성하고 `checklist.md`를 작성한다 (per D-01 마크다운 AI/문서용, D-02 컴포넌트별 분류, D-03 KWCAG 2.2 33개 항목).

**문서 구조:**

```markdown
# KWCAG/WCAG 2.1 AA 퍼블리싱 체크리스트

> KWCAG 2.2 확대 적용 (33개 검사항목 기준, 2026년 전자정부 웹사이트 의무)
> Phase 3 컴포넌트 구조와 1:1 대응하는 컴포넌트별 분류

## 사용법
[체크리스트 사용 방법 설명]

## 공통 항목
항목별로 `- [ ]` 체크박스 형태, 각 항목에 KWCAG 번호와 설명 포함:
- 5.1.1 적절한 대체 텍스트 제공
- 5.3.2 콘텐츠의 선형구조
- 5.4.1 색에 무관한 콘텐츠 인식
- 5.4.3 텍스트 콘텐츠의 명도 대비
- 6.1.2 초점 이동과 표시
- 6.4.2 제목 제공
- 7.1.1 기본 언어 표시
- 8.1.1 마크업 오류 방지

## 버튼 (btn)
해당 항목: 5.4.1, 6.1.1, 6.1.3, 6.5.3

## 폼 (form)
해당 항목: 5.3.3, 5.4.1, 6.1.1, 6.5.3, 7.2.1, 7.3.1, 7.3.2

## 카드 (card)
해당 항목: 5.1.1, 5.4.4, 6.4.3

## 테이블 (table)
해당 항목: 5.3.1, 5.4.4, 6.1.1

## 모달 (modal)
해당 항목: 5.3.2, 6.1.1, 6.1.3, 7.2.1, 8.2.1

## 탭 (tab)
해당 항목: 6.1.1, 6.1.3, 6.4.2, 8.2.1

## 페이지네이션 (pagination)
해당 항목: 6.1.1, 6.4.3, 5.1.1

## 브레드크럼 (breadcrumb)
해당 항목: 6.4.3, 5.3.2, 6.1.1

## 보일러플레이트 (boilerplate)
해당 항목: 6.4.1, 7.1.1

## 해당 없음 항목 (참고)
현재 컴포넌트에 해당하지 않는 검사항목: 5.2.1, 5.4.2, 6.1.4, 6.2.1, 6.2.2, 6.3.1, 6.4.4, 6.5.1, 6.5.2, 6.5.4, 7.2.2, 7.3.3, 7.3.4
```

각 체크 항목 형식:
```
- [ ] **5.1.1 적절한 대체 텍스트 제공** — 이미지 `alt` 속성 필수, 장식용 이미지는 `alt=""` + `role="presentation"`
```

KWCAG 2.2 신규 항목(6.1.4, 6.4.4, 6.5.1~6.5.4, 7.2.2, 7.3.3, 7.3.4)은 `[NEW]` 태그로 표시한다.

주의: src/snippets/*.md 접근성 섹션과 내용 중복을 피한다. 체크리스트는 "확인 항목 목록"이고, 스니펫은 "코드 복사용"이다.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && test -f docs/accessibility/checklist.md && grep -c "\- \[ \]" docs/accessibility/checklist.md | xargs -I{} test {} -ge 33 && grep "KWCAG 2.2" docs/accessibility/checklist.md && grep "\[NEW\]" docs/accessibility/checklist.md && grep "## 버튼" docs/accessibility/checklist.md && grep "## 폼" docs/accessibility/checklist.md && grep "## 모달" docs/accessibility/checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/accessibility/checklist.md 파일 존재
    - 체크박스(`- [ ]`) 항목이 33개 이상 (컴포넌트별 중복 매핑 포함)
    - "KWCAG 2.2" 문구 포함
    - KWCAG 2.2 신규 항목에 `[NEW]` 태그 존재
    - 컴포넌트별 섹션 헤딩: 공통, 버튼, 폼, 카드, 테이블, 모달, 탭, 페이지네이션, 브레드크럼, 보일러플레이트
    - 각 항목에 KWCAG 번호(예: 5.1.1, 6.1.1) 포함
  </acceptance_criteria>
  <done>KWCAG 2.2 33개 검사항목이 컴포넌트별로 분류된 마크다운 체크리스트가 docs/accessibility/checklist.md에 존재하고, 각 항목에 KWCAG 번호와 구체적 확인 방법이 기술되어 있다</done>
</task>

</tasks>

<verification>
1. `node -e "const c = require('./.pa11yci.js'); console.log(c.defaults.standard, c.urls.length)"` → `WCAG2AA 9`
2. `grep "test:a11y" package.json` → 스크립트 존재
3. `wc -l docs/accessibility/checklist.md` → 100줄 이상
4. `grep -c "\- \[ \]" docs/accessibility/checklist.md` → 33개 이상
</verification>

<success_criteria>
- pa11y-ci 설정이 완료되어 `npm run test:a11y`로 playground 9개 HTML을 WCAG2AA 기준 자동 검사 가능
- KWCAG 2.2 33개 검사항목이 컴포넌트별로 분류된 마크다운 체크리스트가 존재
- 리포트 출력 디렉토리(reports/)가 준비되고 .gitignore에 리포트 파일 제외 설정 완료
</success_criteria>

<output>
After completion, create `.planning/phases/04-accessibility-checklist-validation/04-01-SUMMARY.md`
</output>
