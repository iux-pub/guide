---
phase: 25-a11y-token-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tokens.json
  - src/scss/1-settings/_tokens-misc.scss
  - src/scss/6-components/_modal.scss
  - src/scss/6-components/_pagination.scss
  - src/scss/6-components/_breadcrumb.scss
autonomous: true
requirements:
  - TOKFIX-01
  - A11YFIX-01
  - A11YFIX-02
  - A11YFIX-03
  - A11YFIX-04
must_haves:
  truths:
    - "모달 닫기 버튼의 터치 타겟이 44x44px(4.4rem) 이상이다"
    - "페이지네이션 링크의 터치 타겟이 44x44px(4.4rem) 이상이다"
    - "페이지네이션 링크 간 간격이 8px(var(--spacing-sm)) 이상이다"
    - "브레드크럼 아이템 간 간격이 8px(var(--spacing-sm)) 이상이다"
    - "--transition-fast 토큰 값이 0.1s ease로 interaction-timing.md 문서와 일치한다"
  artifacts:
    - path: "tokens.json"
      provides: "transition.fast 싱글 소스 값"
      contains: "0.1s ease"
    - path: "src/scss/1-settings/_tokens-misc.scss"
      provides: "자동 생성된 --transition-fast CSS 변수"
      contains: "--transition-fast: 0.1s ease"
    - path: "src/scss/6-components/_modal.scss"
      provides: "모달 닫기 버튼 44px 터치 타겟"
      contains: "4.4rem"
    - path: "src/scss/6-components/_pagination.scss"
      provides: "페이지네이션 44px 터치 타겟 + 8px 간격"
      contains: "4.4rem"
    - path: "src/scss/6-components/_breadcrumb.scss"
      provides: "브레드크럼 8px 간격"
      contains: "--spacing-sm"
  key_links:
    - from: "tokens.json"
      to: "src/scss/1-settings/_tokens-misc.scss"
      via: "npm run build:tokens"
      pattern: "transition-fast.*0\\.1s"
---

<objective>
토큰 값 정합(--transition-fast)과 터치 타겟/간격 접근성 수정을 수행한다.

Purpose: WCAG 2.1 AA 터치 타겟 44px 최소 기준 충족, 간격 8px 보장, 토큰-문서 간 값 불일치 해소
Output: tokens.json 수정 + 3개 컴포넌트 SCSS 파일 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-a11y-token-fix/25-RESEARCH.md

@tokens.json
@src/scss/1-settings/_tokens-misc.scss
@src/scss/6-components/_modal.scss
@src/scss/6-components/_pagination.scss
@src/scss/6-components/_breadcrumb.scss
@site/design/interaction-timing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: --transition-fast 토큰 값 정합 (TOKFIX-01)</name>
  <files>tokens.json, src/scss/1-settings/_tokens-misc.scss</files>
  <read_first>tokens.json, src/scss/1-settings/_tokens-misc.scss, site/design/interaction-timing.md</read_first>
  <action>
tokens.json에서 transition.fast의 $value를 "0.15s ease"에서 "0.1s ease"로 변경한다.

구체적 변경:
- tokens.json 97행: `"fast": { "$value": "0.15s ease", "$type": "transition" }` -> `"fast": { "$value": "0.1s ease", "$type": "transition" }`

변경 후 `npm run build:tokens` 실행하여 `src/scss/1-settings/_tokens-misc.scss`가 자동 재생성되었는지 확인한다.

주의: `_tokens-misc.scss`는 자동 생성 파일이므로 직접 수정하지 말 것. tokens.json만 수정하고 빌드 스크립트로 반영한다.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && grep '"fast": { "\$value": "0.1s ease"' tokens.json && npm run build:tokens && grep 'transition-fast: 0.1s ease' src/scss/1-settings/_tokens-misc.scss</automated>
  </verify>
  <acceptance_criteria>
- tokens.json에서 `grep '"0.1s ease"' tokens.json` 실행 시 transition.fast 행이 출력된다
- `_tokens-misc.scss`에서 `grep 'transition-fast: 0.1s ease' src/scss/1-settings/_tokens-misc.scss` 실행 시 해당 행이 출력된다
- interaction-timing.md의 duration/fast 100ms와 토큰 0.1s(100ms)가 일치한다
  </acceptance_criteria>
  <done>--transition-fast 토큰이 tokens.json과 SCSS 모두에서 0.1s ease로 통일되어, interaction-timing.md 문서의 100ms와 정합된다</done>
</task>

<task type="auto">
  <name>Task 2: 모달/페이지네이션 터치 타겟 44px 수정 (A11YFIX-01, A11YFIX-02, A11YFIX-03)</name>
  <files>src/scss/6-components/_modal.scss, src/scss/6-components/_pagination.scss</files>
  <read_first>src/scss/6-components/_modal.scss, src/scss/6-components/_pagination.scss</read_first>
  <action>
**_modal.scss 수정 (A11YFIX-01):**
`&__close` 블록의 width와 height를 `4rem`에서 `4.4rem`으로 변경한다.
- 75행: `width: 4rem;` -> `width: 4.4rem;`
- 76행: `height: 4rem;` -> `height: 4.4rem;`
- 기존 주석 "닫기 버튼" 옆에 "-- 터치 타겟 44px 보장" 주석을 추가한다

**_pagination.scss 수정 (A11YFIX-02, A11YFIX-03):**
`&__list` 블록의 gap을 `var(--spacing-xs)`에서 `var(--spacing-sm)`으로 변경한다.
- 16행: `gap: var(--spacing-xs);` -> `gap: var(--spacing-sm);`

`&__link` 블록의 min-width와 height를 `4rem`에서 `4.4rem`으로 변경한다.
- 39행: `min-width: 4rem;` -> `min-width: 4.4rem;`
- 40행: `height: 4rem;` -> `height: 4.4rem;`

주의: BEM 구조와 기존 들여쓰기(2 spaces) 유지. 토큰 사용 필수.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && grep '4.4rem' src/scss/6-components/_modal.scss && grep '4.4rem' src/scss/6-components/_pagination.scss && grep 'spacing-sm' src/scss/6-components/_pagination.scss | grep gap</automated>
  </verify>
  <acceptance_criteria>
- `grep 'width: 4.4rem' src/scss/6-components/_modal.scss` 실행 시 2행(width, height) 출력
- `grep '4.4rem' src/scss/6-components/_pagination.scss` 실행 시 min-width와 height 2행 출력
- `grep 'gap: var(--spacing-sm)' src/scss/6-components/_pagination.scss` 실행 시 1행 출력
- `grep '4rem' src/scss/6-components/_modal.scss | grep -v '4.4rem'` 실행 시 __close 관련 행이 없다 (모두 4.4rem으로 변경됨)
  </acceptance_criteria>
  <done>모달 닫기 버튼과 페이지네이션 링크의 터치 타겟이 44px(4.4rem)이며, 페이지네이션 링크 간 간격이 8px(--spacing-sm)이다</done>
</task>

<task type="auto">
  <name>Task 3: 브레드크럼 간격 8px 수정 (A11YFIX-04)</name>
  <files>src/scss/6-components/_breadcrumb.scss</files>
  <read_first>src/scss/6-components/_breadcrumb.scss</read_first>
  <action>
**_breadcrumb.scss 수정 (A11YFIX-04):**

`&__list` 블록의 gap을 `var(--spacing-xs)`에서 `var(--spacing-sm)`으로 변경한다.
- 19행: `gap: var(--spacing-xs);` -> `gap: var(--spacing-sm);`

`&__item`의 `&:not(:first-child)::before` 블록에서 margin-right를 `var(--spacing-xs)`에서 `var(--spacing-sm)`으로 변경한다.
- 32행: `margin-right: var(--spacing-xs);` -> `margin-right: var(--spacing-sm);`

주의: gap과 ::before margin-right 두 곳 모두 변경해야 시각적 간격이 균일하다. 하나만 변경하면 불균형 발생.

변경 후 `npm run lint:css` 실행하여 린트 통과를 확인한다.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && grep 'spacing-sm' src/scss/6-components/_breadcrumb.scss && npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
- `grep 'spacing-sm' src/scss/6-components/_breadcrumb.scss` 실행 시 최소 2행 출력 (gap + margin-right)
- `grep 'spacing-xs' src/scss/6-components/_breadcrumb.scss` 실행 시 0행 출력 (모두 sm으로 변경됨)
- `npm run lint:css` 실행 시 에러 없이 통과
  </acceptance_criteria>
  <done>브레드크럼 __list gap과 __item::before margin-right가 모두 var(--spacing-sm)(8px)으로 통일되어 아이템 간 간격이 8px 이상이다</done>
</task>

</tasks>

<verification>
1. `npm run build:tokens` 실행 후 `_tokens-misc.scss`에 `--transition-fast: 0.1s ease` 확인
2. `grep '4.4rem' src/scss/6-components/_modal.scss` -- 닫기 버튼 width/height 44px
3. `grep '4.4rem' src/scss/6-components/_pagination.scss` -- 링크 min-width/height 44px
4. `grep 'spacing-sm' src/scss/6-components/_pagination.scss | grep gap` -- 링크 간격 8px
5. `grep 'spacing-sm' src/scss/6-components/_breadcrumb.scss` -- 아이템 간격 8px (2곳)
6. `npm run lint:css` -- 전체 SCSS 린트 통과
</verification>

<success_criteria>
- tokens.json의 transition.fast가 "0.1s ease"이고, 빌드된 SCSS에도 동일하게 반영됨
- 모달 닫기 버튼 width/height가 4.4rem(44px)
- 페이지네이션 링크 min-width/height가 4.4rem(44px), gap이 var(--spacing-sm)(8px)
- 브레드크럼 list gap과 item::before margin-right가 모두 var(--spacing-sm)(8px)
- npm run lint:css 통과
</success_criteria>

<output>
After completion, create `.planning/phases/25-a11y-token-fix/25-01-SUMMARY.md`
</output>
