# Plan 03-02: 폼, 카드, 테이블, 브레드크럼, 페이지네이션 컴포넌트 (SCSS + Playground)

---
wave: 2
depends_on: [03-01]
requirements_addressed: [COMP-02, COMP-03, COMP-04, COMP-07, COMP-08, COMP-11]
files_modified:
  - src/scss/6-components/_form.scss
  - src/scss/6-components/_card.scss
  - src/scss/6-components/_table.scss
  - src/scss/6-components/_breadcrumb.scss
  - src/scss/6-components/_pagination.scss
  - src/scss/6-components/_index.scss
  - src/playground/form.html
  - src/playground/card.html
  - src/playground/table.html
  - src/playground/breadcrumb.html
  - src/playground/pagination.html
autonomous: true
---

<objective>
JS가 필요 없는 정적 컴포넌트 5개(폼, 카드, 테이블, 브레드크럼, 페이지네이션)의 SCSS + playground HTML을 완성한다. BEM 네이밍 + 토큰 + KRDS 접근성 패턴을 내장한다.
</objective>

<tasks>

<task id="03-02-01">
<title>폼 컴포넌트 SCSS + playground HTML</title>
<read_first>
- src/scss/6-components/_btn.scss
- src/scss/6-components/_index.scss
- src/scss/1-settings/_tokens-color.scss
- src/scss/1-settings/_tokens-typography.scss
- src/scss/1-settings/_tokens-spacing.scss
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (폼 섹션)
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-02 섹션)
</read_first>
<action>
1. `src/scss/6-components/_form.scss` 생성:

BEM `.form` 블록:

```scss
// 폼 컴포넌트
@use '../2-tools/responsive' as resp;

.form {
  &__group {
    margin-bottom: var(--spacing-md);
  }

  &__label {
    display: block;
    margin-bottom: var(--spacing-xs);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
    color: var(--color-text);

    &--required::after {
      content: ' *';
      color: var(--color-danger);
    }
  }

  &__input,
  &__select,
  &__textarea {
    display: block;
    width: 100%;
    padding: var(--spacing-sm) var(--spacing-md);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-base);
    font-family: var(--font-family-base);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-regular);
    line-height: var(--leading-base);
    color: var(--color-text);
    background-color: var(--color-bg);
    transition: border-color var(--transition-fast);

    &::placeholder {
      color: var(--color-gray-500);
    }

    &:focus {
      outline: none;
      border-color: var(--color-primary);
      box-shadow: 0 0 0 2px rgba(37, 110, 244, 0.2);
    }

    &:disabled {
      background-color: var(--color-bg-secondary);
      color: var(--color-text-disabled);
      cursor: not-allowed;
    }

    &--error {
      border-color: var(--color-danger);

      &:focus {
        box-shadow: 0 0 0 2px rgba(222, 52, 18, 0.2);
      }
    }

    &--success {
      border-color: var(--color-success);

      &:focus {
        box-shadow: 0 0 0 2px rgba(34, 135, 56, 0.2);
      }
    }
  }

  &__textarea {
    min-height: 12rem;
    resize: vertical;
  }

  &__select {
    appearance: none;
    background-image: url("data:image/svg+xml,..."); // 드롭다운 화살표 SVG
    background-repeat: no-repeat;
    background-position: right var(--spacing-sm) center;
    padding-right: var(--spacing-xl);
  }

  &__help {
    display: block;
    margin-top: var(--spacing-xs);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  &__message {
    display: block;
    margin-top: var(--spacing-xs);
    font-size: var(--font-size-sm);

    &--error {
      color: var(--color-danger);
    }

    &--success {
      color: var(--color-success);
    }
  }

  &__checkbox,
  &__radio {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    cursor: pointer;
  }

  &__checkbox-input,
  &__radio-input {
    width: 1.8rem;
    height: 1.8rem;
    accent-color: var(--color-primary);
  }

  &__checkbox-label,
  &__radio-label {
    font-size: var(--font-size-base);
    color: var(--color-text);
  }
}
```

반응형: 모바일 1열, tablet-up에서 `.form--inline` modifier로 가로 배치 옵션 제공.

2. `_index.scss`에 `@forward 'form';` 추가

3. `src/playground/form.html` 생성:
- `<title>`: "폼 컴포넌트 - 미리보기"
- 섹션별:
  - 텍스트 입력 (label + input, placeholder "내용을 입력하세요")
  - 필수 입력 (`form__label--required`)
  - 에러 상태 (`form__input--error` + `aria-invalid="true"` + `aria-describedby` + `role="alert"` 에러 메시지 "필수 입력 항목입니다.")
  - 성공 상태 (`form__input--success` + 성공 메시지 "입력이 확인되었습니다.")
  - 비활성 (`disabled`)
  - Select 드롭다운
  - Textarea
  - 체크박스
  - 라디오 버튼
- 각 섹션 렌더링 + 코드 표시

4. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_form.scss` 파일 존재
- 파일에 `.form {` 포함
- 파일에 `&__group` 포함
- 파일에 `&__label` 포함
- 파일에 `&__input` 포함
- 파일에 `&__select` 포함
- 파일에 `&__textarea` 포함
- 파일에 `&__message--error` 포함
- 파일에 `&__message--success` 포함
- 파일에 `&__checkbox` 포함
- 파일에 `&__radio` 포함
- 파일에 `var(--color-danger)` 포함
- 파일에 `var(--color-success)` 포함
- `_index.scss`에 `@forward 'form'` 포함
- `src/playground/form.html` 파일 존재
- form.html에 `aria-invalid="true"` 포함
- form.html에 `aria-describedby` 포함
- form.html에 `role="alert"` 포함
- form.html에 `필수 입력 항목입니다` 포함
- form.html에 `pg__preview` 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

<task id="03-02-02">
<title>카드 컴포넌트 SCSS + playground HTML</title>
<read_first>
- src/scss/6-components/_btn.scss
- src/scss/6-components/_index.scss
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (카드 섹션)
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-03 섹션)
</read_first>
<action>
1. `src/scss/6-components/_card.scss` 생성:

BEM `.card` 블록:
- `.card`: `border: 1px solid var(--color-border); border-radius: var(--radius-base); background-color: var(--color-bg); overflow: hidden`
- `.card__header`: `padding: var(--spacing-md); border-bottom: 1px solid var(--color-border-light)`
- `.card__title`: `font-size: var(--font-size-lg); font-weight: var(--font-weight-bold); color: var(--color-text); margin: 0`
- `.card__body`: `padding: var(--spacing-md)`
- `.card__text`: `font-size: var(--font-size-base); color: var(--color-text); line-height: var(--leading-base)`
- `.card__footer`: `padding: var(--spacing-md); border-top: 1px solid var(--color-border-light); display: flex; gap: var(--spacing-sm); justify-content: flex-end`
- `.card__media`: overflow hidden, 이미지 래퍼
- `.card__image`: `width: 100%; height: auto; display: block; object-fit: cover`

Variant modifiers:
- `.card--horizontal`: `display: flex`. 모바일에서 `flex-direction: column`, tablet-up에서 `flex-direction: row`. `.card__media` 너비 tablet-up에서 `width: 30%; flex-shrink: 0`
- `.card--image`: 이미지가 상단에 오는 기본 세로 레이아웃 (기본과 동일, `.card__media`가 첫 번째 자식)
- `.card--featured`: `border-color: var(--color-primary); border-width: 2px`

2. `_index.scss`에 `@forward 'card';` 추가

3. `src/playground/card.html` 생성:
- `<title>`: "카드 컴포넌트 - 미리보기"
- 섹션: 기본 카드 (`<article>` 태그), 가로형 카드, 이미지형 카드, Featured 카드
- `<article>` 시맨틱 태그 사용
- 각 섹션 렌더링 + 코드 표시

4. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_card.scss` 파일 존재
- 파일에 `.card {` 포함
- 파일에 `&__header` 포함
- 파일에 `&__title` 포함
- 파일에 `&__body` 포함
- 파일에 `&__footer` 포함
- 파일에 `&__image` 포함
- 파일에 `&--horizontal` 포함
- 파일에 `&--featured` 포함
- 파일에 `var(--color-primary)` 포함
- 파일에 `respond-to` 포함
- `_index.scss`에 `@forward 'card'` 포함
- `src/playground/card.html` 파일 존재
- card.html에 `<article` 포함
- card.html에 `card--horizontal` 포함
- card.html에 `card--featured` 포함
- card.html에 `pg__preview` 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

<task id="03-02-03">
<title>테이블 컴포넌트 SCSS + playground HTML</title>
<read_first>
- src/scss/6-components/_btn.scss
- src/scss/6-components/_index.scss
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (테이블 섹션)
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-04 섹션)
</read_first>
<action>
1. `src/scss/6-components/_table.scss` 생성:

BEM `.table` 블록:
- `.table-wrap`: `overflow-x: auto; -webkit-overflow-scrolling: touch` (모바일 가로 스크롤 래퍼, 주의: `table-wrap`은 BEM block이며 `table__wrapper`가 아닌 독립 블록)
  - 또는 UI-SPEC 대로 `.table__wrapper` 사용 (table 블록의 element)
- `.table`: `width: 100%; border-collapse: collapse; font-size: var(--font-size-sm)`
- `.table__head`: 테이블 헤더 영역
- `.table__th`: `padding: var(--spacing-md); text-align: left; font-weight: var(--font-weight-bold); color: var(--color-text); border-bottom: 2px solid var(--color-border); white-space: nowrap`
- `.table__body`: 테이블 본문
- `.table__row`: 행
- `.table__td`: `padding: var(--spacing-md); border-bottom: 1px solid var(--color-border-light); color: var(--color-text); font-weight: var(--font-weight-regular)`
- `.table__empty`: `text-align: center; padding: var(--spacing-2xl); color: var(--color-text-secondary)` (빈 상태 메시지 셀)

Variant modifiers:
- `.table--striped`: `.table__body .table__row:nth-child(even)` → `background-color: var(--color-bg-secondary)`
- `.table--bordered`: `.table__th, .table__td` → `border: 1px solid var(--color-border-light)`

2. `_index.scss`에 `@forward 'table';` 추가

3. `src/playground/table.html` 생성:
- `<title>`: "테이블 컴포넌트 - 미리보기"
- 섹션:
  - 기본 테이블: `<caption class="sr-only">사용자 목록</caption>` + `<th scope="col">` 필수
  - Striped 테이블
  - Bordered 테이블
  - 반응형 (`.table__wrapper` 래퍼로 가로 스크롤)
  - 빈 상태 ("등록된 데이터가 없습니다. 새 항목을 추가해 주세요.")
- 데모 데이터: 이름, 이메일, 역할 컬럼, 3~5행
- 각 섹션 렌더링 + 코드 표시

4. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_table.scss` 파일 존재
- 파일에 `.table {` 포함 또는 `.table` 블록 정의
- 파일에 `&__th` 포함
- 파일에 `&__td` 포함
- 파일에 `&__row` 포함
- 파일에 `&--striped` 포함
- 파일에 `&--bordered` 포함
- 파일에 `var(--color-border)` 포함
- `_index.scss`에 `@forward 'table'` 포함
- `src/playground/table.html` 파일 존재
- table.html에 `<caption` 포함
- table.html에 `scope="col"` 포함
- table.html에 `table--striped` 포함
- table.html에 `등록된 데이터가 없습니다` 포함
- table.html에 `pg__preview` 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

<task id="03-02-04">
<title>브레드크럼 컴포넌트 SCSS + playground HTML</title>
<read_first>
- src/scss/6-components/_index.scss
- src/scss/6-components/_btn.scss
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-08 섹션)
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (브레드크럼 섹션)
</read_first>
<action>
1. `src/scss/6-components/_breadcrumb.scss` 생성:

BEM `.breadcrumb` 블록:
- `.breadcrumb`: `<nav aria-label="현재 위치">`
- `.breadcrumb__list`: `display: flex; flex-wrap: wrap; align-items: center; list-style: none; padding: 0; margin: 0; gap: var(--spacing-xs)`
- `.breadcrumb__item`: `display: flex; align-items: center; font-size: var(--font-size-sm); line-height: var(--leading-base)`
  - `&:not(:first-child)::before`: `content: '/'; margin-right: var(--spacing-xs); color: var(--color-gray-400)` (CSS pseudo-element 구분자)
- `.breadcrumb__link`: `color: var(--color-primary); text-decoration: none; font-weight: var(--font-weight-regular); transition: color var(--transition-fast)`
  - hover: `text-decoration: underline`
  - focus-visible: `outline: 2px solid var(--color-primary); outline-offset: 2px`
- `.breadcrumb__current`: `color: var(--color-text); font-weight: var(--font-weight-regular)` (aria-current="page")

반응형: 모바일에서 마지막 2단계만 표시 (나머지 `display: none`), `tablet-up`에서 전체 `display: flex`. `@use '../2-tools/responsive' as resp;` 사용.

2. `_index.scss`에 `@forward 'breadcrumb';` 추가

3. `src/playground/breadcrumb.html` 생성:
- `<title>`: "브레드크럼 컴포넌트 - 미리보기"
- 섹션: 기본 (3단계), 긴 경로 (5단계), `aria-current="page"` 현재 위치
- `<nav aria-label="현재 위치">` + `<ol>` (순서 있는 목록) 필수
- 현재 항목: `<span class="breadcrumb__current" aria-current="page">`

4. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_breadcrumb.scss` 파일 존재
- 파일에 `.breadcrumb {` 포함
- 파일에 `&__list` 포함
- 파일에 `&__item` 포함
- 파일에 `&__link` 포함
- 파일에 `&__current` 포함
- 파일에 `&:focus-visible` 포함
- 파일에 `respond-to` 포함
- `_index.scss`에 `@forward 'breadcrumb'` 포함
- `src/playground/breadcrumb.html` 파일 존재
- breadcrumb.html에 `aria-label` 포함
- breadcrumb.html에 `aria-current="page"` 포함
- breadcrumb.html에 `<ol` 포함
- breadcrumb.html에 `pg__preview` 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

<task id="03-02-05">
<title>페이지네이션 컴포넌트 SCSS + playground HTML</title>
<read_first>
- src/scss/6-components/_index.scss
- src/scss/6-components/_btn.scss
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-07 섹션)
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (페이지네이션 섹션)
</read_first>
<action>
1. `src/scss/6-components/_pagination.scss` 생성:

BEM `.pagination` 블록:
- `.pagination`: `<nav aria-label="페이지 네비게이션">`
- `.pagination__list`: `display: flex; align-items: center; gap: var(--spacing-xs); list-style: none; padding: 0; margin: 0`
- `.pagination__item`: 각 `<li>`
- `.pagination__link`: `display: inline-flex; align-items: center; justify-content: center; min-width: 4rem; height: 4rem; padding: var(--spacing-xs) var(--spacing-sm); border: 1px solid var(--color-border); border-radius: var(--radius-base); font-size: var(--font-size-sm); color: var(--color-text); text-decoration: none; transition: background-color var(--transition-fast), border-color var(--transition-fast)`
- `.pagination__link:hover:not(.pagination__link--disabled)`: `background-color: var(--color-bg-secondary)`
- `.pagination__link--current`: `background-color: var(--color-primary); color: var(--color-white); border-color: var(--color-primary)` + `aria-current="page"`
- `.pagination__link--prev`, `.pagination__link--next`: 이전/다음 화살표 스타일
- `.pagination__link--disabled`: `opacity: 0.5; cursor: not-allowed; pointer-events: none`
- `&:focus-visible`: `outline: 2px solid var(--color-primary); outline-offset: 2px`

반응형: 모바일에서 숫자 일부 숨김 (이전/다음 + 현재 + 처음/마지막만), `tablet-up`에서 전체 표시. `.pagination__item--mobile-hidden` modifier.

2. `_index.scss`에 `@forward 'pagination';` 추가

3. `src/playground/pagination.html` 생성:
- `<title>`: "페이지네이션 컴포넌트 - 미리보기"
- 섹션: 기본 (1~5), 현재 페이지 활성, 비활성 이전 버튼, 많은 페이지 (1~10+)
- `<nav aria-label="검색 결과 페이지 탐색">` 래퍼
- `aria-label="페이지 N"`, `aria-current="page"`, `aria-label="이전 페이지"/"다음 페이지"`

4. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_pagination.scss` 파일 존재
- 파일에 `.pagination {` 포함
- 파일에 `&__list` 포함
- 파일에 `&__link` 포함
- 파일에 `&__link--current` 포함
- 파일에 `&__link--prev` 포함
- 파일에 `&__link--next` 포함
- 파일에 `&__link--disabled` 포함
- 파일에 `&:focus-visible` 포함
- 파일에 `respond-to` 포함
- `_index.scss`에 `@forward 'pagination'` 포함
- `src/playground/pagination.html` 파일 존재
- pagination.html에 `aria-label` 포함
- pagination.html에 `aria-current="page"` 포함
- pagination.html에 `pg__preview` 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

</tasks>

<verification>
## 검증 기준

- [ ] `npm run lint:css` 통과 (warning 허용)
- [ ] `npm run build:css` 성공
- [ ] `dist/css/style.css`에 `.form`, `.card`, `.table`, `.breadcrumb`, `.pagination` 클래스 포함
- [ ] 폼 HTML에 에러/성공 상태 aria 속성 내장
- [ ] 카드 HTML에 `<article>` 시맨틱 태그 사용
- [ ] 테이블 HTML에 `<caption>` + `scope="col"` 시맨틱 마크업
- [ ] 브레드크럼 HTML에 `<nav>` + `<ol>` + `aria-current="page"`
- [ ] 페이지네이션 HTML에 `<nav>` + `aria-current="page"` + 반응형 모바일 숨김
- [ ] 5개 playground HTML 브라우저 렌더링 정상
</verification>

<must_haves>
- 폼 전체 요소 (input/select/textarea/checkbox/radio + error/success/disabled 상태)
- 카드 variant (기본/가로/이미지/featured + 반응형)
- 테이블 variant (기본/striped/bordered + 반응형 스크롤 + 빈 상태)
- 브레드크럼 (nav, ol, aria-current="page", 반응형)
- 페이지네이션 (nav, aria-current="page", 이전/다음, 반응형)
- 모든 컴포넌트 BEM 네이밍 + 토큰 사용
- KRDS 접근성 패턴 (aria-invalid, aria-describedby, caption, scope, aria-current)
</must_haves>
