---
phase: 05-documentation-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eleventy.config.js
  - package.json
  - .gitignore
  - site/_data/site.json
  - site/_data/navigation.json
  - site/_includes/layouts/base.njk
  - site/_includes/layouts/page.njk
  - site/_includes/layouts/component.njk
  - site/_includes/partials/nav-sidebar.njk
  - site/_includes/partials/nav-mobile.njk
  - site/_includes/partials/search.njk
  - site/index.md
  - site/assets/js/nav-mobile.js
  - src/scss/docs.scss
autonomous: true
requirements:
  - DOCS-01
  - DOCS-06

must_haves:
  truths:
    - "npx @11ty/eleventy --dryrun 실행 시 에러 없이 완료된다"
    - "npm run build:site 실행 시 _site/ 디렉토리에 index.html이 생성된다"
    - "문서 사이트에 skip-to-content 링크가 존재한다"
    - "좌측 사이드바 네비게이션에 4개 대섹션이 표시된다"
    - "모바일에서 햄버거 메뉴가 동작한다"
  artifacts:
    - path: "eleventy.config.js"
      provides: "Eleventy ESM 설정"
      contains: "export default"
    - path: "site/_includes/layouts/base.njk"
      provides: "HTML 셸 레이아웃"
      contains: "skip-to-content"
    - path: "site/_data/navigation.json"
      provides: "사이드바 네비게이션 데이터"
      contains: "tokens"
    - path: "src/scss/docs.scss"
      provides: "문서 사이트 전용 스타일"
      contains: "docs-layout"
  key_links:
    - from: "site/_includes/layouts/base.njk"
      to: "/assets/css/style.css"
      via: "link rel=stylesheet"
      pattern: "assets/css/style\\.css"
    - from: "site/_includes/layouts/base.njk"
      to: "/assets/css/docs.css"
      via: "link rel=stylesheet"
      pattern: "assets/css/docs\\.css"
    - from: "eleventy.config.js"
      to: "site/"
      via: "dir.input 설정"
      pattern: "input.*site"
---

<objective>
Eleventy 기반 문서 사이트 인프라를 구축한다. 패키지 설치, Eleventy ESM 설정, Nunjucks 레이아웃 3종(base/page/component), 사이드바+모바일 네비게이션, 문서 전용 SCSS 파일을 생성하여 콘텐츠 페이지를 추가할 수 있는 기반을 완성한다.

Purpose: Phase 5의 모든 후속 플랜(콘텐츠 페이지, 검색, 접근성 검증)이 이 인프라 위에서 동작한다.
Output: 빌드 가능한 Eleventy 프로젝트 + 랜딩 페이지 + 네비게이션 레이아웃
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-site/05-CONTEXT.md
@.planning/phases/05-documentation-site/05-RESEARCH.md
@package.json
@.gitignore
@src/scss/style.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eleventy 패키지 설치 + 설정 + 빌드 스크립트 + 문서 SCSS</name>
  <files>
    package.json,
    eleventy.config.js,
    .gitignore,
    src/scss/docs.scss,
    site/_data/site.json,
    site/index.md
  </files>
  <read_first>
    package.json,
    .gitignore,
    src/scss/style.scss,
    src/scss/1-settings/_index.scss,
    src/scss/2-tools/_index.scss,
    .planning/phases/05-documentation-site/05-RESEARCH.md (Pattern 1: Eleventy ESM 설정, npm scripts 패턴)
  </read_first>
  <action>
    1. npm install로 패키지 설치 (per D-04 clipboard.js, per RESEARCH standard stack):
       ```
       npm install --save-dev @11ty/eleventy@^3.1.5 @11ty/eleventy-plugin-syntaxhighlight@^5.0.2 pagefind concurrently
       npm install clipboard
       ```

    2. eleventy.config.js 생성 (ESM, per RESEARCH Pattern 1):
       - `import syntaxHighlight from '@11ty/eleventy-plugin-syntaxhighlight'`
       - `eleventyConfig.addPlugin(syntaxHighlight)`
       - passthrough copy: `site/assets` -> assets, `dist/css` -> `assets/css`, `src/playground` -> `playground`
       - `eleventy.after` 이벤트에서 pagefind CLI 실행: `npx -y pagefind --site _site --glob "**/*.html"`
       - dir 설정: input='site', output='_site', includes='_includes', data='_data'
       - templateFormats: ['md', 'njk', 'html'], markdownTemplateEngine: 'njk', htmlTemplateEngine: 'njk'

    3. package.json scripts 추가 (기존 스크립트 유지):
       - `"build:docs-css": "sass src/scss/docs.scss dist/css/docs.css --load-path=node_modules"`
       - `"build:site": "npm run build:css && npm run build:docs-css && npx @11ty/eleventy"`
       - `"serve": "concurrently \"sass src/scss/style.scss dist/css/style.css --load-path=node_modules --watch\" \"sass src/scss/docs.scss dist/css/docs.css --load-path=node_modules --watch\" \"npx @11ty/eleventy --serve\""`

    4. .gitignore에 추가: `_site/`, `_site/pagefind/`

    5. src/scss/docs.scss 생성:
       - `@use '1-settings' as settings;` 로 토큰 참조
       - `@use '2-tools' as tools;` 로 믹스인 참조
       - 문서 레이아웃 BEM 블록: `.docs-layout`, `.docs-layout__sidebar`, `.docs-layout__content`
       - 사이드바 스타일: `.docs-nav`, `.docs-nav__section`, `.docs-nav__link`, `.docs-nav__link--active`
       - 모바일 메뉴: `.docs-mobile-header`, `.docs-mobile-header__toggle`
       - 반응형: 모바일에서 사이드바 숨김(display:none), PC에서 사이드바 고정(position:sticky) (per D-06)
       - 토큰 기반 스타일링: 모든 색상/간격/타이포 값은 CSS Custom Properties 사용 (per D-05)
       - skip-to-content 스타일 (sr-only + 포커스 시 표시)
       - 코드 블록(pre/code) 기본 스타일, `.copy-btn` 스타일 (우측 상단 배치, per D-04)

    6. site/_data/site.json 생성:
       ```json
       {
         "title": "INFOMIND UX 가이드",
         "description": "인포마인드 UX팀 디자인/퍼블리싱 가이드 시스템",
         "url": ""
       }
       ```

    7. site/index.md 생성 (랜딩 페이지):
       - front matter: layout=layouts/page.njk, title=홈
       - 가이드 시스템 소개 콘텐츠: 4개 섹션 안내 (토큰, 컨벤션, 컴포넌트, 접근성)
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npx @11ty/eleventy --dryrun 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - eleventy.config.js 파일에 `export default` 포함
    - package.json에 `build:site`, `serve`, `build:docs-css` 스크립트 존재
    - src/scss/docs.scss에 `.docs-layout` 블록 존재
    - site/index.md에 `layout: layouts/page.njk` front matter 존재
    - .gitignore에 `_site/` 포함
  </acceptance_criteria>
  <done>Eleventy 설정 완료, dryrun 에러 없음, docs.scss 빌드 가능, 랜딩 페이지 마크다운 존재</done>
</task>

<task type="auto">
  <name>Task 2: Nunjucks 레이아웃 3종 + 네비게이션(사이드바+모바일) 구현</name>
  <files>
    site/_data/navigation.json,
    site/_includes/layouts/base.njk,
    site/_includes/layouts/page.njk,
    site/_includes/layouts/component.njk,
    site/_includes/partials/nav-sidebar.njk,
    site/_includes/partials/nav-mobile.njk,
    site/_includes/partials/search.njk,
    site/assets/js/nav-mobile.js
  </files>
  <read_first>
    eleventy.config.js (Task 1에서 생성),
    site/_data/site.json (Task 1에서 생성),
    src/scss/docs.scss (Task 1에서 생성),
    .planning/phases/05-documentation-site/05-RESEARCH.md (Pattern 2: Nunjucks 레이아웃, Pattern 6: pagefind 검색)
  </read_first>
  <action>
    1. site/_data/navigation.json 생성 — 4개 대섹션 구조 (per D-01):
       ```json
       {
         "sections": [
           {
             "title": "토큰",
             "key": "tokens",
             "items": [
               { "title": "개요", "url": "/tokens/" },
               { "title": "색상", "url": "/tokens/color/" },
               { "title": "타이포그래피", "url": "/tokens/typography/" },
               { "title": "간격", "url": "/tokens/spacing/" },
               { "title": "그리드", "url": "/tokens/grid/" }
             ]
           },
           {
             "title": "컨벤션",
             "key": "conventions",
             ...BEM, SCSS 구조
           },
           {
             "title": "컴포넌트",
             "key": "components",
             ...8개 컴포넌트 + 보일러플레이트
           },
           {
             "title": "접근성",
             "key": "accessibility",
             ...체크리스트, 색상대비, sr-only, 컴포넌트별 가이드
           }
         ]
       }
       ```

    2. base.njk — HTML 셸 (per RESEARCH Pattern 2, per DOCS-06 접근성):
       - `<!DOCTYPE html><html lang="ko">` (per 접근성)
       - head: charset, viewport, title="{{ title }} | {{ site.title }}", style.css + docs.css 링크, pagefind-ui.css 링크
       - `<a href="#main-content" class="skip-to-content sr-only">본문 바로가기</a>` (per DOCS-06)
       - nav-mobile.njk include
       - `.docs-layout` 래퍼 안에 nav-sidebar.njk + `<main id="main-content" class="docs-layout__content" data-pagefind-body>`
       - scripts: pagefind-ui.js, clipboard-init.js, nav-mobile.js
       - `{% block scripts %}{% endblock %}`

    3. page.njk — 기본 페이지 레이아웃:
       - `{% extends "layouts/base.njk" %}`
       - `{% block content %}` 안에 `<h1>{{ title }}</h1>` + `{{ content | safe }}`

    4. component.njk — 컴포넌트 미리보기 레이아웃:
       - `{% extends "layouts/base.njk" %}`
       - `{% block content %}` 안에 title, 미리보기 iframe(playground_src front matter 참조), 코드 예제 영역
       - iframe에 `title` 속성 필수 (접근성), `loading="lazy"`, 고정 높이

    5. nav-sidebar.njk — 좌측 사이드바 (per D-02 Claude 재량):
       - `<nav class="docs-nav" role="navigation" aria-label="문서 탐색">`
       - search.njk include (검색 위젯 자리)
       - navigation.json 순회하여 섹션별 링크 렌더링
       - 현재 페이지 활성 표시: `page.url`과 비교하여 `docs-nav__link--active` 적용

    6. nav-mobile.njk — 모바일 햄버거 메뉴 (per D-06):
       - `.docs-mobile-header` 안에 사이트 제목 + 햄버거 버튼
       - 버튼: `aria-expanded="false"`, `aria-controls="mobile-nav"`, `aria-label="메뉴 열기"`
       - PC에서 숨김 (CSS)

    7. search.njk — pagefind 검색 위젯 자리 (per RESEARCH Pattern 6):
       - `<div class="docs-search" role="search" aria-label="문서 검색"><div id="search"></div></div>`
       - PagefindUI 초기화 스크립트 (translations: placeholder="검색어를 입력하세요", zero_results="검색 결과가 없습니다")

    8. site/assets/js/nav-mobile.js — 모바일 메뉴 토글 (바닐라 JS IIFE 패턴):
       - 햄버거 버튼 클릭 시 사이드바 표시/숨김
       - aria-expanded 토글
       - ESC 키로 닫기
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run build:css && npm run build:docs-css && npx @11ty/eleventy && test -f _site/index.html && grep -q "skip-to-content" _site/index.html && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - _site/index.html에 `skip-to-content` 링크 포함
    - _site/index.html에 `role="navigation"` 포함
    - _site/index.html에 `aria-label="문서 탐색"` 포함
    - site/_data/navigation.json에 4개 섹션(tokens, conventions, components, accessibility) 존재
    - base.njk에 `lang="ko"` 포함
    - nav-mobile.njk에 `aria-expanded` 포함
  </acceptance_criteria>
  <done>Eleventy 빌드 성공, _site/index.html에 접근성 준수 레이아웃(skip-to-content, ARIA 속성, 사이드바 네비게이션)이 렌더링됨</done>
</task>

</tasks>

<verification>
- `npx @11ty/eleventy --dryrun` 에러 없음
- `npm run build:site` 실행 후 `_site/index.html` 존재
- _site/index.html에 skip-to-content, role="navigation", lang="ko" 포함
- `npm run build:docs-css` 실행 후 dist/css/docs.css 생성
- 사이드바에 4개 섹션 링크 렌더링
</verification>

<success_criteria>
Eleventy 문서 사이트 인프라가 완성되어 `npm run build:site`로 빌드되고, 랜딩 페이지에 사이드바 네비게이션(4개 섹션)과 모바일 메뉴가 동작하며, WCAG AA 기본 접근성(skip-to-content, ARIA, lang) 준수
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-site/05-01-SUMMARY.md`
</output>
