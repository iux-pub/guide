# Plan 03-03: 모달, 탭 컴포넌트 (SCSS + 바닐라 JS + Playground)

---
wave: 2
depends_on: [03-01]
requirements_addressed: [COMP-05, COMP-06, COMP-11]
files_modified:
  - src/scss/6-components/_modal.scss
  - src/scss/6-components/_tab.scss
  - src/scss/6-components/_index.scss
  - src/js/modal.js
  - src/js/tab.js
  - src/playground/modal.html
  - src/playground/tab.html
autonomous: true
---

<objective>
바닐라 JS가 필요한 인터랙티브 컴포넌트 2개(모달, 탭)를 완성한다. SCSS + 바닐라 JS(포커스 트랩, ESC 닫기, 키보드 좌우 전환) + playground HTML을 한 세트로 구현한다. WAI-ARIA APG 패턴을 준수한다.
</objective>

<tasks>

<task id="03-03-01">
<title>모달 컴포넌트 SCSS + JS + playground HTML</title>
<read_first>
- src/scss/6-components/_btn.scss
- src/scss/6-components/_index.scss
- src/scss/1-settings/_tokens-color.scss
- src/scss/1-settings/_tokens-misc.scss
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (모달 섹션, Interaction States)
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-05 섹션, Pattern 4)
</read_first>
<action>
1. `src/scss/6-components/_modal.scss` 생성:

BEM `.modal` 블록:
- `.modal`: `display: none; position: fixed; inset: 0; z-index: var(--z-modal); align-items: center; justify-content: center`. 열릴 때 JS가 `display: flex` 설정
- `.modal[aria-hidden="false"]`: `display: flex`
- `.modal__overlay`: `position: fixed; inset: 0; background-color: rgba(0, 0, 0, 0.5); z-index: var(--z-modal-backdrop); transition: opacity var(--transition-base)`
- `.modal__container`: `position: relative; z-index: var(--z-modal); background-color: var(--color-bg); border-radius: var(--radius-lg); box-shadow: var(--shadow-lg); width: 100%; max-height: 90vh; overflow-y: auto`
  - 모바일: `width: 100%; height: 100%; max-height: 100%; border-radius: 0` (전체 화면)
  - tablet-up: `max-width: 56rem; margin: var(--spacing-lg)`
- `.modal__header`: `display: flex; align-items: center; justify-content: space-between; padding: var(--spacing-lg); border-bottom: 1px solid var(--color-border-light)`
- `.modal__title`: `font-size: var(--font-size-lg); font-weight: var(--font-weight-bold); color: var(--color-text); margin: 0`
- `.modal__close`: `display: inline-flex; align-items: center; justify-content: center; width: 4rem; height: 4rem; border: none; background: transparent; cursor: pointer; border-radius: var(--radius-base); color: var(--color-text-secondary); transition: background-color var(--transition-fast)`. hover: `background-color: var(--color-bg-secondary)`. focus-visible: `outline: 2px solid var(--color-primary); outline-offset: 2px`
- `.modal__body`: `padding: var(--spacing-lg)`
- `.modal__footer`: `display: flex; gap: var(--spacing-sm); justify-content: flex-end; padding: var(--spacing-lg); border-top: 1px solid var(--color-border-light)`

`@use '../2-tools/responsive' as resp;` 사용하여 반응형 적용.

2. `src/js/modal.js` 생성:

바닐라 JS IIFE 패턴:
```javascript
// 모달 컴포넌트 — 포커스 트랩 + ESC 닫기
;(function () {
  'use strict'

  var FOCUSABLE = 'a[href], button:not([disabled]), textarea:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])'

  function openModal(modal, trigger) {
    modal.setAttribute('aria-hidden', 'false')
    modal.style.display = 'flex'
    document.body.style.overflow = 'hidden'
    var firstFocusable = modal.querySelector(FOCUSABLE)
    if (firstFocusable) firstFocusable.focus()
    modal._trigger = trigger
  }

  function closeModal(modal) {
    modal.setAttribute('aria-hidden', 'true')
    modal.style.display = 'none'
    document.body.style.overflow = ''
    if (modal._trigger) modal._trigger.focus()
  }

  function trapFocus(modal, e) {
    var focusables = modal.querySelectorAll(FOCUSABLE)
    if (focusables.length === 0) return
    var first = focusables[0]
    var last = focusables[focusables.length - 1]
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault()
      last.focus()
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault()
      first.focus()
    }
  }

  // 이벤트 위임 — 클릭
  document.addEventListener('click', function (e) {
    var trigger = e.target.closest('[data-modal-open]')
    if (trigger) {
      var modal = document.getElementById(trigger.dataset.modalOpen)
      if (modal) openModal(modal, trigger)
    }
    var close = e.target.closest('[data-modal-close]')
    if (close) {
      var modal = close.closest('.modal')
      if (modal) closeModal(modal)
    }
    if (e.target.classList.contains('modal__overlay')) {
      var modal = e.target.closest('.modal')
      if (modal) closeModal(modal)
    }
  })

  // 이벤트 위임 — 키보드
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') {
      var openModal_ = document.querySelector('.modal[aria-hidden="false"]')
      if (openModal_) closeModal(openModal_)
    }
    if (e.key === 'Tab') {
      var openModal_ = document.querySelector('.modal[aria-hidden="false"]')
      if (openModal_) trapFocus(openModal_, e)
    }
  })
})()
```

3. `_index.scss`에 `@forward 'modal';` 추가

4. `src/playground/modal.html` 생성:
- `<title>`: "모달 컴포넌트 - 미리보기"
- `<script src="../../src/js/modal.js"></script>` 로드
- 섹션:
  - 기본 모달: 트리거 버튼 (`data-modal-open`) → 모달 (`role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-hidden="true"`)
    - 모달 제목 id 연결 (`aria-labelledby`)
    - 닫기 버튼 (`aria-label="닫기"`, `data-modal-close`)
    - 오버레이 클릭 닫기
    - Footer: "작성 취소" (outline) + "저장하기" (primary)
  - 삭제 확인 모달: "정말 삭제하시겠습니까?" 메시지 + "항목 삭제" (danger 스타일) + "작성 취소"
- 코드 표시 포함

5. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_modal.scss` 파일 존재
- 파일에 `.modal {` 포함
- 파일에 `&__overlay` 포함
- 파일에 `&__container` 포함
- 파일에 `&__header` 포함
- 파일에 `&__title` 포함
- 파일에 `&__close` 포함
- 파일에 `&__body` 포함
- 파일에 `&__footer` 포함
- 파일에 `var(--z-modal)` 포함
- 파일에 `respond-to` 포함
- `src/js/modal.js` 파일 존재
- modal.js에 `aria-hidden` 포함
- modal.js에 `trapFocus` 또는 `trap` 포함
- modal.js에 `Escape` 포함
- modal.js에 `data-modal-open` 포함
- modal.js에 `data-modal-close` 포함
- `_index.scss`에 `@forward 'modal'` 포함
- `src/playground/modal.html` 파일 존재
- modal.html에 `role="dialog"` 포함
- modal.html에 `aria-modal="true"` 포함
- modal.html에 `aria-labelledby` 포함
- modal.html에 `aria-hidden="true"` 포함
- modal.html에 `aria-label="닫기"` 또는 `aria-label="모달 닫기"` 포함
- modal.html에 `modal.js` 스크립트 참조 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

<task id="03-03-02">
<title>탭 컴포넌트 SCSS + JS + playground HTML</title>
<read_first>
- src/scss/6-components/_btn.scss
- src/scss/6-components/_index.scss
- .planning/phases/03-components-html-scss-snippets/03-UI-SPEC.md (탭 섹션, Interaction States)
- .planning/phases/03-components-html-scss-snippets/03-RESEARCH.md (COMP-06 섹션)
</read_first>
<action>
1. `src/scss/6-components/_tab.scss` 생성:

BEM `.tab` 블록:
- `.tab`: 탭 전체 래퍼
- `.tab__list`: `display: flex; border-bottom: 2px solid var(--color-border-light); gap: 0; overflow-x: auto` (`role="tablist"`)
  - 모바일: `overflow-x: auto; -webkit-overflow-scrolling: touch; flex-wrap: nowrap` (가로 스크롤)
- `.tab__button`: `padding: var(--spacing-sm) var(--spacing-md); border: none; border-bottom: 2px solid transparent; margin-bottom: -2px; background: transparent; font-family: var(--font-family-base); font-size: var(--font-size-base); font-weight: var(--font-weight-bold); color: var(--color-text-secondary); cursor: pointer; white-space: nowrap; transition: color var(--transition-fast), border-color var(--transition-fast)`
  - hover: `color: var(--color-text)`
  - focus-visible: `outline: 2px solid var(--color-primary); outline-offset: -2px`
- `.tab__button--active` 또는 `.tab__button[aria-selected="true"]`: `color: var(--color-primary); border-bottom-color: var(--color-primary)`
- `.tab__panel`: `padding: var(--spacing-lg) 0` (`role="tabpanel"`)
- `.tab__panel[hidden]`: `display: none`

2. `src/js/tab.js` 생성:

바닐라 JS IIFE 패턴 — WAI-ARIA APG Tabs 자동 활성화 패턴:
```javascript
// 탭 컴포넌트 — 키보드 좌우 전환 (자동 활성화)
;(function () {
  'use strict'

  function activateTab(tab) {
    var tablist = tab.closest('[role="tablist"]')
    var tabs = tablist.querySelectorAll('[role="tab"]')
    var tabContainer = tablist.closest('.tab')

    // 모든 탭 비활성화
    tabs.forEach(function (t) {
      t.setAttribute('aria-selected', 'false')
      t.setAttribute('tabindex', '-1')
    })

    // 선택된 탭 활성화
    tab.setAttribute('aria-selected', 'true')
    tab.setAttribute('tabindex', '0')
    tab.focus()

    // 패널 전환
    var panelId = tab.getAttribute('aria-controls')
    var panels = tabContainer.querySelectorAll('[role="tabpanel"]')
    panels.forEach(function (panel) {
      panel.hidden = panel.id !== panelId
    })
  }

  // 이벤트 위임 — 클릭
  document.addEventListener('click', function (e) {
    var tab = e.target.closest('[role="tab"]')
    if (tab) activateTab(tab)
  })

  // 이벤트 위임 — 키보드
  document.addEventListener('keydown', function (e) {
    var tab = e.target.closest('[role="tab"]')
    if (!tab) return

    var tablist = tab.closest('[role="tablist"]')
    var tabs = Array.from(tablist.querySelectorAll('[role="tab"]'))
    var index = tabs.indexOf(tab)

    var newTab = null
    if (e.key === 'ArrowRight') {
      newTab = tabs[(index + 1) % tabs.length]
    } else if (e.key === 'ArrowLeft') {
      newTab = tabs[(index - 1 + tabs.length) % tabs.length]
    } else if (e.key === 'Home') {
      newTab = tabs[0]
    } else if (e.key === 'End') {
      newTab = tabs[tabs.length - 1]
    }

    if (newTab) {
      e.preventDefault()
      activateTab(newTab)
    }
  })
})()
```

3. `_index.scss`에 `@forward 'tab';` 추가

4. `src/playground/tab.html` 생성:
- `<title>`: "탭 컴포넌트 - 미리보기"
- `<script src="../../src/js/tab.js"></script>` 로드
- 섹션:
  - 기본 탭 (3개 탭): `role="tablist"` + `role="tab"` + `aria-selected` + `aria-controls` + `role="tabpanel"` + `aria-labelledby`
    - 첫 번째 탭: `aria-selected="true"`, `tabindex="0"`
    - 나머지 탭: `aria-selected="false"`, `tabindex="-1"`
    - 패널: `id="tabpanel-{name}"`, `aria-labelledby="tab-{name}"`, 비활성 패널에 `hidden`
  - 많은 탭 (5개+): 모바일 가로 스크롤 데모
- 코드 표시 포함
- ID 패턴: `tab-{name}-{n}` / `tabpanel-{name}-{n}`

5. `npm run lint:css && npm run build:css` 실행 확인
</action>
<acceptance_criteria>
- `src/scss/6-components/_tab.scss` 파일 존재
- 파일에 `.tab {` 포함
- 파일에 `&__list` 포함
- 파일에 `&__button` 포함
- 파일에 `&__panel` 포함
- 파일에 `var(--color-primary)` 포함
- 파일에 `aria-selected` 포함
- `src/js/tab.js` 파일 존재
- tab.js에 `aria-selected` 포함
- tab.js에 `ArrowRight` 포함
- tab.js에 `ArrowLeft` 포함
- tab.js에 `aria-controls` 포함
- tab.js에 `Home` 포함
- tab.js에 `End` 포함
- `_index.scss`에 `@forward 'tab'` 포함
- `src/playground/tab.html` 파일 존재
- tab.html에 `role="tablist"` 포함
- tab.html에 `role="tab"` 포함
- tab.html에 `role="tabpanel"` 포함
- tab.html에 `aria-selected="true"` 포함
- tab.html에 `aria-controls` 포함
- tab.html에 `aria-labelledby` 포함
- tab.html에 `tab.js` 스크립트 참조 포함
- `npm run build:css` exit code 0
</acceptance_criteria>
</task>

</tasks>

<verification>
## 검증 기준

- [ ] `npm run lint:css` 통과
- [ ] `npm run build:css` 성공
- [ ] `dist/css/style.css`에 `.modal`, `.tab` 클래스 포함
- [ ] 모달: 열기/닫기 동작, ESC 닫기, 오버레이 클릭 닫기, 포커스 트랩 (Tab 순환)
- [ ] 탭: 클릭 전환, ArrowLeft/ArrowRight 키보드 전환, Home/End 이동
- [ ] 모든 ARIA 속성 올바르게 동기화 (aria-hidden, aria-selected, hidden)
</verification>

<must_haves>
- 모달: overlay + container + header/body/footer + focus trap + ESC 닫기 + 트리거 포커스 복귀
- 탭: tablist + tab + tabpanel + 키보드 좌우/Home/End + 자동 활성화
- 바닐라 JS (프레임워크 비의존, IIFE 패턴, 이벤트 위임)
- WAI-ARIA APG 패턴 준수 (dialog-modal, tabs)
- 모달 반응형: 모바일 전체 화면, tablet-up 중앙 정렬 max-width 560px
</must_haves>
