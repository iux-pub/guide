---
phase: 04-accessibility-checklist-validation
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/playground/a11y-checklist.html
  - src/js/a11y-checklist.js
  - .pa11yci.js
autonomous: false
requirements: [A11Y-01, A11Y-02, A11Y-06]
user_setup: []

must_haves:
  truths:
    - "HTML 인터랙티브 체크리스트에서 항목을 체크하면 localStorage에 저장되고 새로고침 후에도 유지된다"
    - "진행률 표시가 체크 항목 수에 따라 업데이트된다"
    - "npm run test:a11y 실행 시 모든 playground 페이지가 pa11y-ci 검사를 통과한다"
  artifacts:
    - path: "src/playground/a11y-checklist.html"
      provides: "KWCAG 2.2 인터랙티브 체크리스트 HTML"
      min_lines: 100
    - path: "src/js/a11y-checklist.js"
      provides: "체크리스트 인터랙션 JS (localStorage, 진행률)"
      contains: "localStorage"
  key_links:
    - from: "src/playground/a11y-checklist.html"
      to: "src/js/a11y-checklist.js"
      via: "script 태그"
      pattern: "a11y-checklist.js"
    - from: ".pa11yci.js"
      to: "src/playground/a11y-checklist.html"
      via: "URL 목록에 추가"
      pattern: "a11y-checklist.html"
---

<objective>
KWCAG 2.2 HTML 인터랙티브 체크리스트 페이지를 만들고, pa11y-ci로 모든 playground 페이지의 접근성을 검증한다.

Purpose: 실무자가 브라우저에서 체크하며 사용하는 인터랙티브 체크리스트 + 전체 컴포넌트 접근성 자동 검증
Output: a11y-checklist.html, a11y-checklist.js, pa11y-ci 검증 통과
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accessibility-checklist-validation/04-CONTEXT.md
@.planning/phases/04-accessibility-checklist-validation/04-RESEARCH.md
@.planning/phases/04-accessibility-checklist-validation/04-01-SUMMARY.md

<interfaces>
<!-- Phase 3 playground HTML 패턴 참조 -->
<!-- pg__ 접두사 레이아웃, dist/css/style.css 참조, style 태그 내 pg 전용 스타일 -->

From docs/accessibility/checklist.md (Plan 01에서 생성):
<!-- 마크다운 체크리스트의 항목 구조를 HTML로 변환 -->
<!-- 컴포넌트별 섹션: 공통, 버튼, 폼, 카드, 테이블, 모달, 탭, 페이지네이션, 브레드크럼, 보일러플레이트 -->

From .pa11yci.js (Plan 01에서 생성):
```javascript
// urls 배열에 a11y-checklist.html 추가 필요
const pages = [
  'index.html',
  'btn.html',
  // ...
  'a11y-checklist.html'  // 추가
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML 인터랙티브 체크리스트 페이지 + JS 작성</name>
  <files>src/playground/a11y-checklist.html, src/js/a11y-checklist.js</files>
  <read_first>
    - src/playground/btn.html (playground 페이지 구조 패턴 확인: pg__ 접두사, head, CSS 참조)
    - docs/accessibility/checklist.md (마크다운 체크리스트 항목 — HTML로 변환 기반)
    - src/js/modal.js (바닐라 JS IIFE 패턴 확인)
    - .planning/phases/04-accessibility-checklist-validation/04-RESEARCH.md (Pattern 5: HTML 체크리스트 구조)
  </read_first>
  <action>
**1. src/playground/a11y-checklist.html 생성 (per D-01 HTML 실무 체크용):**

Phase 3 playground 패턴을 따른다:
- `<!DOCTYPE html>`, `<html lang="ko">`, viewport meta, skip-to-content
- `<link rel="stylesheet" href="../../dist/css/style.css">`
- `<style>` 태그로 `pg__` 접두사 레이아웃 스타일
- BEM 클래스명 사용: `a11y-checklist` 블록

HTML 구조 (per D-02 컴포넌트별 분류, Research Pattern 5):
```html
<main id="main-content">
  <h1 class="pg__title">KWCAG/WCAG 2.1 AA 퍼블리싱 체크리스트</h1>
  <p class="pg__desc">KWCAG 2.2 확대 적용 (33개 검사항목)</p>

  <!-- 진행률 표시 -->
  <div class="a11y-checklist__progress" role="progressbar"
       aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"
       aria-label="체크리스트 진행률">
    <div class="a11y-checklist__progress-bar"></div>
    <span class="a11y-checklist__progress-text">0 / N 완료</span>
  </div>

  <!-- 리셋 버튼 -->
  <button type="button" class="btn btn--outline a11y-checklist__reset">
    체크리스트 초기화
  </button>

  <!-- 섹션: 공통 항목 -->
  <fieldset class="a11y-checklist__section">
    <legend class="a11y-checklist__title">공통 항목</legend>
    <label class="a11y-checklist__item">
      <input type="checkbox" class="a11y-checklist__check"
             data-category="common" data-id="5-1-1">
      <span class="a11y-checklist__label">
        <strong>5.1.1</strong> 적절한 대체 텍스트 제공 — 이미지 alt 속성 필수
      </span>
    </label>
    <!-- ... 나머지 항목 ... -->
  </fieldset>

  <!-- 컴포넌트별 섹션: 버튼, 폼, 카드, 테이블, 모달, 탭, 페이지네이션, 브레드크럼, 보일러플레이트 -->
</main>
```

docs/accessibility/checklist.md의 모든 항목을 HTML checkbox로 변환한다. KWCAG 2.2 신규 항목은 `[NEW]` 배지 표시.

`<style>` 태그에 pg__ 전용 스타일 + a11y-checklist BEM 스타일:
- 진행률 바: 배경 `var(--color-gray-100)`, 채움 `var(--color-success)`
- 체크된 항목: `text-decoration: line-through`, `opacity: 0.7`
- 섹션 간격, 체크박스 정렬 등

**2. src/js/a11y-checklist.js 생성:**

Phase 3 바닐라 JS 패턴(IIFE, use strict, 이벤트 위임):
```javascript
;(function () {
  'use strict'

  // localStorage 키
  const STORAGE_KEY = 'a11y-checklist-state'

  // 체크 상태 저장/복원
  function saveState() { ... }
  function loadState() { ... }

  // 진행률 업데이트
  function updateProgress() {
    const total = document.querySelectorAll('.a11y-checklist__check').length
    const checked = document.querySelectorAll('.a11y-checklist__check:checked').length
    const percent = Math.round((checked / total) * 100)
    // progressbar 업데이트
  }

  // 이벤트 위임
  document.addEventListener('change', function(e) {
    if (e.target.matches('.a11y-checklist__check')) {
      saveState()
      updateProgress()
    }
  })

  // 리셋 버튼
  document.addEventListener('click', function(e) {
    if (e.target.matches('.a11y-checklist__reset')) {
      // confirm 후 초기화
    }
  })

  // 페이지 로드 시 상태 복원
  document.addEventListener('DOMContentLoaded', function() {
    loadState()
    updateProgress()
  })
})()
```

a11y-checklist.html 하단에 `<script src="../../src/js/a11y-checklist.js"></script>` 추가.

주의:
- 체크리스트 페이지 자체도 접근성 준수: fieldset/legend, label 연결, progressbar role
- BEM 네이밍: `a11y-checklist__*`
- 토큰 사용: 색상, 간격 등 CSS Custom Properties
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && test -f src/playground/a11y-checklist.html && test -f src/js/a11y-checklist.js && grep "localStorage" src/js/a11y-checklist.js && grep "progressbar" src/playground/a11y-checklist.html && grep "a11y-checklist__check" src/playground/a11y-checklist.html && grep "lang=\"ko\"" src/playground/a11y-checklist.html && grep "fieldset" src/playground/a11y-checklist.html</automated>
  </verify>
  <acceptance_criteria>
    - src/playground/a11y-checklist.html 파일 존재, `lang="ko"` 포함
    - fieldset/legend로 섹션 구분
    - 체크박스가 label로 감싸져 있음
    - progressbar role 속성 포함
    - data-category, data-id 속성으로 항목 식별
    - src/js/a11y-checklist.js에 localStorage 저장/복원 로직
    - 진행률 업데이트 로직 (aria-valuenow 동적 변경)
    - IIFE 패턴, 'use strict'
    - BEM 클래스명 a11y-checklist__* 사용
  </acceptance_criteria>
  <done>HTML 인터랙티브 체크리스트에서 항목 체크 시 localStorage 저장, 새로고침 복원, 진행률 표시가 동작한다</done>
</task>

<task type="auto">
  <name>Task 2: pa11y-ci URL 추가 + 전체 접근성 검증 실행 + 이슈 수정</name>
  <files>.pa11yci.js, src/playground/*.html (이슈 발견 시 수정)</files>
  <read_first>
    - .pa11yci.js (현재 URL 목록 확인)
    - package.json (test:a11y 스크립트 확인)
  </read_first>
  <action>
1. `.pa11yci.js`의 pages 배열에 `'a11y-checklist.html'` 추가.

2. `npm run build:css`로 CSS 빌드 (pa11y-ci 검사 전 필수).

3. `npm run test:a11y` 실행하여 전체 playground 10개 HTML 검사.

4. 에러가 발생하면:
   - 각 에러의 WCAG 규칙과 해당 요소 확인
   - playground HTML 파일에서 해당 접근성 이슈 수정
   - 수정 후 다시 `npm run test:a11y` 실행
   - 모든 에러가 해결될 때까지 반복

5. 최종 결과를 확인하고, reports/a11y-report.json이 생성되었는지 확인.

주의:
- pa11y-ci가 disabled 요소의 대비 비율을 지적할 수 있음 — WCAG에서 disabled 요소는 예외이므로 해당 규칙을 ignore 설정에 추가 가능
- 모달 닫힌 상태의 aria-hidden 관련 경고는 정상 동작 (Pitfall 5)
- CSS 빌드 없이 실행하면 스타일 미적용 상태에서 잘못된 에러 발생 (Pitfall 2)
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run test:a11y</automated>
  </verify>
  <acceptance_criteria>
    - .pa11yci.js의 pages 배열에 'a11y-checklist.html' 포함 (총 10개)
    - `npm run test:a11y` 실행 시 에러 0개 (warning은 허용)
    - reports/a11y-report.json 파일 생성
    - 10개 playground HTML 모두 WCAG2AA 기준 통과
  </acceptance_criteria>
  <done>pa11y-ci가 playground 10개 HTML을 WCAG2AA 기준으로 검사하고 모든 페이지가 통과한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 접근성 체크리스트 + pa11y-ci 결과 확인</name>
  <files>없음 (검증 전용)</files>
  <action>
사용자에게 다음을 확인 요청:
1. 브라우저에서 src/playground/a11y-checklist.html 열어 체크박스 UI 동작 확인
2. localStorage 저장/복원 동작 확인
3. npm run test:a11y 결과 에러 0 확인
  </action>
  <what-built>
    - HTML 인터랙티브 체크리스트 (src/playground/a11y-checklist.html)
    - pa11y-ci 자동 검증으로 전체 playground 접근성 통과
  </what-built>
  <how-to-verify>
    1. 브라우저에서 `src/playground/a11y-checklist.html` 열기
    2. 체크박스를 몇 개 체크 -> 진행률 표시 업데이트 확인
    3. 페이지 새로고침 -> 체크 상태 유지 확인 (localStorage)
    4. "체크리스트 초기화" 버튼 클릭 -> 모든 체크 해제 확인
    5. 터미널에서 `npm run test:a11y` 결과 확인 -> 에러 0
  </how-to-verify>
  <verify>사용자 승인</verify>
  <done>사용자가 체크리스트 UI 동작과 pa11y-ci 결과를 확인하고 승인</done>
  <resume-signal>"approved" 입력 또는 수정 필요사항 설명</resume-signal>
</task>

</tasks>

<verification>
1. `npm run test:a11y` -> 10개 페이지 모두 통과 (0 errors)
2. 브라우저에서 a11y-checklist.html 체크박스 + localStorage 동작
3. `node -e "const c = require('./.pa11yci.js'); console.log(c.urls.length)"` -> 10
</verification>

<success_criteria>
- HTML 인터랙티브 체크리스트가 localStorage 저장, 진행률 표시와 함께 동작
- pa11y-ci가 playground 10개 HTML을 WCAG2AA 기준으로 검사하고 모든 페이지 통과
- 사용자가 체크리스트 UI와 pa11y-ci 결과를 확인하고 승인
</success_criteria>

<output>
After completion, create `.planning/phases/04-accessibility-checklist-validation/04-03-SUMMARY.md`
</output>
