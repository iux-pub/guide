---
phase: 27-docs-playground-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/components/card.md
  - site/components/modal.md
  - site/components/form.md
  - site/components/tab.md
  - site/components/pagination.md
  - site/components/breadcrumb.md
  - site/components/table.md
  - site/components/btn.md
  - site/guides/css-animation.md
  - site/design-qa/tolerance.md
  - site/prompts/publishing.md
autonomous: true
requirements: [SIZE-04, TOKFIX-02]

must_haves:
  truths:
    - "site/ 문서 페이지가 변경된 SCSS 규칙(애니메이션, hover, 반응형, 포커스 링, reduced-motion)을 반영한다"
    - "site/guides/css-animation.md의 --transition-fast 값이 0.1s로 갱신된다"
    - "playground HTML이 npm run build:css로 최신 CSS를 반영한다"
    - "npm test가 통과한다"
  artifacts:
    - path: "site/components/modal.md"
      provides: "모달 열림 애니메이션 문서화"
      contains: "modal--active"
    - path: "site/components/card.md"
      provides: "카드 hover 효과 문서화"
      contains: "translateY"
    - path: "site/guides/css-animation.md"
      provides: "--transition-fast 0.1s 갱신"
      contains: "0.1s"
  key_links:
    - from: "site/guides/css-animation.md"
      to: "tokens.json"
      via: "토큰 값 정합"
      pattern: "0\\.1s"
    - from: "site/components/modal.md"
      to: "src/scss/6-components/_modal.scss"
      via: "modal--active 클래스 문서화"
      pattern: "modal--active"
---

<objective>
site/ 문서 사이트 페이지를 Phase 25-26 SCSS 변경에 맞게 갱신하고, CSS 빌드로 playground를 반영한 뒤, npm test로 최종 검증한다.

Purpose: 문서 사이트가 가이드 시스템의 최종 산출물 -- 여기가 불일치하면 팀원이 잘못된 정보를 참조한다.
Output: site/ 컴포넌트/가이드 페이지 갱신, playground CSS 빌드, npm test 통과
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-a11y-token-fix/25-01-SUMMARY.md
@.planning/phases/25-a11y-token-fix/25-02-SUMMARY.md
@.planning/phases/26-scss-modernize/26-01-SUMMARY.md
@.planning/phases/26-scss-modernize/26-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: site/ 컴포넌트 + 가이드 문서 갱신</name>
  <files>site/components/card.md, site/components/modal.md, site/components/form.md, site/components/tab.md, site/components/pagination.md, site/components/breadcrumb.md, site/components/table.md, site/components/btn.md, site/guides/css-animation.md, site/design-qa/tolerance.md, site/prompts/publishing.md</files>
  <read_first>site/components/card.md, site/components/modal.md, site/components/form.md, site/components/tab.md, site/components/pagination.md, site/components/breadcrumb.md, site/components/table.md, site/components/btn.md, site/guides/css-animation.md, site/design-qa/tolerance.md, site/prompts/publishing.md</read_first>
  <action>
**site/components/modal.md:**
- 구성 요소 테이블에 행 추가: "| 활성 상태 | `.modal--active` | JS가 열 때 추가, 열림 애니메이션 트리거 |"
- "## 접근성 주의사항" 뒤에 "## 열림 애니메이션" 섹션 추가:
  ```
  ## 열림 애니메이션

  JS에서 `modal--active` 클래스를 추가하면 `@keyframes modal-open` 애니메이션이 실행된다.

  - scale(0.95) -> scale(1) + opacity 0 -> 1
  - Duration: 300ms ease
  - `prefers-reduced-motion: reduce` 설정 시 `animation: none` 처리
  ```
- Close 행 설명에 "(44x44px 터치 타겟)" 추가
- "## 반응형" 섹션 추가: "모바일: 전체화면 / 태블릿: max-width 56rem, max-height 90vh / PC: max-height 85vh, 패딩 확대"

**site/components/card.md:**
- "## 접근성 주의사항" 뒤에 "## 인터랙션" 섹션 추가:
  ```
  ## 인터랙션

  - hover 시 `box-shadow: var(--shadow-lg)` + `transform: translateY(-0.2rem)` 효과
  - `transition: box-shadow, transform var(--transition-base)`
  - `prefers-reduced-motion: reduce` 시 transition 비활성화
  - 반응형 패딩: header/body/footer가 모바일/태블릿/PC에서 차등 적용
  ```

**site/components/form.md:**
- 접근성 주의사항에 추가:
  - ":focus-visible + outline: 2px solid + outline-offset: 2px 포커스 링 (고대비 모드 호환)"
  - "prefers-reduced-motion 대응"
  - "반응형 패딩: 인풋 영역이 모바일/태블릿/PC에서 차등"
  - "레이블 폰트 16px(--font-size-base)"

**site/components/tab.md:**
- 접근성 주의사항에 추가: "focus-visible: outline 2px solid + outline-offset 2px"
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"
- 구성 요소 Button 설명에 패딩 정보: "패딩 12px/20px (모바일), 반응형 확대"

**site/components/pagination.md (있으면 수정, 없으면 확인):**
- 접근성 주의사항에 추가: "링크 최소 44x44px 터치 타겟"
- 접근성 주의사항에 추가: "링크 간 간격 8px (--spacing-sm)"
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"

**site/components/breadcrumb.md (있으면 수정):**
- 접근성 주의사항에 추가: "아이템 간 간격 8px (--spacing-sm)"
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"
- 접근성 주의사항에 추가: "태블릿 이상 폰트 16px"

**site/components/table.md (있으면 수정):**
- 접근성 주의사항에 추가: "기본 폰트 16px"
- 접근성 주의사항에 추가: "반응형 패딩: th/td 모바일/태블릿/PC 차등"

**site/components/btn.md (있으면 수정):**
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"
- 접근성 주의사항에 추가: "반응형 패딩 차등"

**site/guides/css-animation.md -- --transition-fast 값 수정:**
- `0.15s ease` -> `0.1s ease` (3곳: 토큰 표, 설명 문단, Do 예제 주석)
  - 찾기: `| \`var(--transition-fast)\` | 0.15s ease |` -> `| \`var(--transition-fast)\` | 0.1s ease |`
  - 찾기: `**\`--transition-fast\` (0.15s)**` -> `**\`--transition-fast\` (0.1s)**`
  - 찾기: `transition: background-color 0.15s ease-in-out;` -> 이것은 DON'T 예제이므로 유지 (의도적 하드코딩 예시)

**site/design-qa/tolerance.md:**
- 찾기: `0.15s/0.3s/0.5s 토큰 우선` -> `0.1s/0.3s/0.5s 토큰 우선`

**site/prompts/publishing.md (사이트 버전 -- prompts/publishing.md와 별도):**
- 읽어서 `0.15s` 참조가 있으면 `0.1s`로 갱신
  </action>
  <verify>
    <automated>grep -q "modal--active" site/components/modal.md && grep -q "translateY" site/components/card.md && grep -q "focus-visible" site/components/form.md && grep -rL "0.15s" site/guides/css-animation.md > /dev/null 2>&1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "modal--active" site/components/modal.md -- 매칭
    - grep "translateY" site/components/card.md -- 매칭
    - grep "focus-visible" site/components/form.md -- 매칭
    - grep "prefers-reduced-motion" site/components/btn.md -- 매칭
    - grep "0.15s ease" site/guides/css-animation.md -- 토큰 설명에서 매칭 없음 (DON'T 예제의 하드코딩 제외)
    - grep "0.1s ease" site/guides/css-animation.md -- 매칭
    - grep "0.1s" site/design-qa/tolerance.md -- 매칭
  </acceptance_criteria>
  <done>site/ 문서의 컴포넌트 페이지가 최신 SCSS 규칙을 반영하고, css-animation.md와 tolerance.md의 토큰 값이 0.1s로 정합된다</done>
</task>

<task type="auto">
  <name>Task 2: CSS 빌드 + npm test 최종 검증</name>
  <files></files>
  <read_first></read_first>
  <action>
1. `npm run build:css` 실행하여 playground HTML에 최신 SCSS가 CSS로 반영되도록 한다.
   - playground HTML 파일은 CSS를 link로 참조하므로, CSS만 재빌드하면 자동으로 최신 스타일이 반영된다.

2. `npm test` 실행하여 lint + a11y 테스트 통과를 확인한다.
   - 만약 lint 에러가 발생하면 해당 파일을 수정한다.
   - 만약 a11y 에러가 발생하면 해당 페이지를 수정한다.

3. 테스트 통과 후, build 결과물에 변경이 있다면 커밋에 포함한다.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run build:css && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm run build:css 성공 (exit 0)
    - npm test 성공 (exit 0) -- lint + a11y 모두 통과
  </acceptance_criteria>
  <done>playground가 최신 CSS를 반영하고 npm test(lint + a11y)가 통과한다</done>
</task>

</tasks>

<verification>
1. site/ 컴포넌트 페이지에 Phase 25-26 변경사항 반영 확인
2. site/guides/css-animation.md에서 0.15s -> 0.1s 갱신 확인
3. npm run build:css 성공
4. npm test 통과
</verification>

<success_criteria>
1. site/ 문서 페이지가 변경된 규칙(애니메이션, hover, 포커스 링, reduced-motion, 반응형 패딩, 터치 타겟)을 반영
2. --transition-fast 값이 site/ 전체에서 0.1s로 통일 (DON'T 예제 제외)
3. npm run build:css 성공 (playground 반영)
4. npm test 통과
</success_criteria>

<output>
After completion, create `.planning/phases/27-docs-playground-sync/27-02-SUMMARY.md`
</output>
