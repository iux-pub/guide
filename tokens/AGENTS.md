# Token Scope

이 경로에서는 `tokens/foundation.json`이 단일 원본이다.

1. 변경 전 `.agents/skills/change-token/SKILL.md` 절차를 따른다.
2. 기존 semantic token으로 해결 가능한지 먼저 확인한다.
3. primitive와 semantic alias의 역할을 섞지 않는다.
4. `tokens/build/tokens.css`는 직접 수정하지 않는다.
5. 변경 후 `npm run build:tokens`와 사용처 검색을 수행한다.
