---
phase: 28-claude-md-slim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - SLIM-01
  - SLIM-02
must_haves:
  truths:
    - "CLAUDE.md 총 라인 수가 280줄 이하 (기존 557줄 대비 50% 감소)"
    - "BEM 네이밍 규칙, 하드코딩 금지, ITCSS 레이어 규칙이 그대로 유지되어 있다"
    - "토큰 값 나열이 파일 경로 참조로 대체되어 있다"
    - "글로벌 CLAUDE.md와 프로젝트 CLAUDE.md 간 중복이 없다"
    - "GSD 마커가 모두 보존되어 있다"
  artifacts:
    - path: "CLAUDE.md"
      provides: "경량화된 프로젝트 CLAUDE.md"
      contains: "GSD:project-start"
  key_links:
    - from: "CLAUDE.md"
      to: "src/scss/1-settings/_tokens-*.scss"
      via: "파일 경로 참조"
      pattern: "src/scss/1-settings/_tokens"
---

<objective>
CLAUDE.md를 557줄에서 280줄 이하로 경량화한다. 토큰 값 나열을 파일 참조로 대체하고, 글로벌 CLAUDE.md와의 중복을 제거하며, Technology Stack 섹션을 핵심만 남긴다.

Purpose: Claude가 매 대화마다 로딩하는 CLAUDE.md의 컨텍스트 비용을 50% 이상 줄여 응답 품질을 유지한다.
Output: 경량화된 CLAUDE.md (280줄 이하)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md 경량화 — 토큰 값 제거 + 중복 제거 + 스택 축약</name>
  <files>CLAUDE.md</files>
  <action>
CLAUDE.md를 Read한 후, 아래 섹션별 지침에 따라 경량화된 버전을 Write한다.

**반드시 보존할 GSD 마커 (6개 쌍):**
- `<!-- GSD:project-start -->` / `<!-- GSD:project-end -->`
- `<!-- GSD:stack-start -->` / `<!-- GSD:stack-end -->`
- `<!-- GSD:conventions-start -->` / `<!-- GSD:conventions-end -->`
- `<!-- GSD:architecture-start -->` / `<!-- GSD:architecture-end -->`
- `<!-- GSD:workflow-start -->` / `<!-- GSD:workflow-end -->`
- `<!-- GSD:profile-start -->` / `<!-- GSD:profile-end -->`

---

**섹션 1: Project (lines 1-18) — 유지**
그대로 유지. 변경 없음.

**섹션 2: Technology Stack (lines 20-114) — 대폭 축약 (SLIM-02)**
현재 ~94줄 → 목표 ~20줄.

제거할 내용:
- 각 기술의 "Why" 컬럼 내용 (선택 이유는 이미 결정됨, 매번 읽을 필요 없음)
- "Alternatives Considered" 테이블 전체 (이미 결정된 스택)
- "Storybook", "Astro", "Docusaurus" 등 거부된 대안 설명 전체
- "Installation", "Key npm Scripts", "Sources" 섹션 전체 (비어있거나 package.json에 있음)

유지할 내용:
- 기술명 + 버전만 심플 리스트로:
```
## Technology Stack

| Category | Technology | Version |
|----------|-----------|---------|
| Site Generator | Eleventy (11ty) | ^3.1.5 |
| Template | Nunjucks | ^3.2.4 |
| CSS Preprocessor | sass (Dart Sass) | ^1.98.0 |
| PostCSS | PostCSS + Autoprefixer | ^8.5.0 / ^10.4.0 |
| SCSS Pattern | ITCSS | - |
| Tokens | Style Dictionary | ^5.4.0 |
| Linting | Stylelint + stylelint-selector-bem-pattern | ^17.5.0 |
| A11y Testing | pa11y-ci + axe-core | latest / ^4.11.0 |
| Build | npm scripts + concurrently | - |
```
- `sass`가 올바른 npm 패키지명이라는 주의사항 1줄: `> sass가 올바른 npm 패키지 (dart-sass 아님). @use/@forward 사용 (@import 금지).`

**섹션 3: Conventions (lines 116-392) — 토큰 값 제거 + 중복 제거 (SLIM-01, SLIM-02)**

3-1. BEM 네이밍 규칙 (### 1) — 유지하되 축약
- 필수 패턴 테이블: 유지
- SCSS 작성 규칙 4줄: 유지
- 금지 패턴 테이블: 유지
- SCSS BEM 중첩 예시 코드블록 (lines 151-178): 제거. 금지 패턴 테이블이 충분한 판단 기준 제공.

3-2. CSS 규칙 (### 2) — 제거 (SLIM-02)
글로벌 CLAUDE.md에 동일 내용 있음 (BEM 준수, 인라인 금지, !important 금지).
프로젝트 고유 규칙만 남김: "CSS Custom Properties(토큰)를 우선 사용하라. 하드코딩 값을 금지한다" — 이것은 "### 3. 디자인 토큰 사용법"으로 이동.

3-3. 디자인 토큰 사용법 (### 3) — 핵심만 남김 (SLIM-01)
제거할 내용 (~112줄 → ~15줄):
- 색상 토큰 값 나열 전체 (lines 194-234의 코드블록)
- 타이포그래피 토큰 값 나열 전체 (lines 238-261)
- 간격 토큰 값 나열 전체 (lines 265-274)
- 기타 토큰 값 나열 전체 (lines 278-303)
- 그리드 토큰 값 나열 전체 (lines 307-312)

유지할 내용:
- "모든 스타일 값은 CSS Custom Properties(토큰)를 사용하라. 하드코딩 색상, 크기, 간격을 금지한다." 규칙 문장
- 토큰 파일 경로 참조 (리스트 형태로):
```
토큰 파일 참조 (값은 각 파일에서 확인):
- 색상: `src/scss/1-settings/_tokens-color.scss`
- 타이포: `src/scss/1-settings/_tokens-typography.scss`
- 간격: `src/scss/1-settings/_tokens-spacing.scss`
- 기타 (radius, shadow, transition, z-index): `src/scss/1-settings/_tokens-misc.scss`
- 그리드: `src/scss/1-settings/_tokens-grid.scss`
```
- 하드코딩 vs 토큰 비교 예시 (lines 316-332): 유지 — 이것이 패턴을 가르치는 핵심 예시
- CSS Custom Properties 우선 사용 규칙 (섹션 2에서 이동한 내용 통합)

3-4. 린트 규칙 (### 4) — 유지
변경 없음. 실행 가능한 명령어와 규칙.

3-5. 코딩 스타일 (### 5) — 제거 (SLIM-02)
글로벌 CLAUDE.md에 동일 내용 있음 (2 spaces, single quote, 세미콜론 없음, 한국어 주석, 커밋 메시지).
단, SCSS 세미콜론 예외 규칙은 린트 규칙 섹션에 1줄 추가: "SCSS에서는 세미콜론 사용"

3-6. 접근성 (### 6) — 제거 (SLIM-02)
글로벌 CLAUDE.md에 기본 접근성 규칙 있음.
프로젝트 고유 추가 규칙 2개만 린트/접근성 참조 경로 부근으로 이동:
- "본문 건너뛰기 링크 제공: `<a href="#main-content" class="skip-to-content">`"
- "스크린 리더 전용 텍스트: `.sr-only` 클래스 사용"

3-7. 컴포넌트 스니펫 (### 7) — 유지
테이블 + 안내 문구 그대로 유지.

**섹션 4: Architecture (lines 394-524) — 유지하되 소폭 축약**
- SCSS 구조 트리 (### 1): 유지 — ITCSS 레이어 이해에 필수
- 새 파일 추가 규칙: 유지
- 모듈 시스템: 유지
- ITCSS 레이어별 BEM 적용 범위 테이블: 유지
- 반응형 작성 (### 2): 유지 — 믹스인 사용법/브레이크포인트는 핵심 규칙
- 공용 믹스인 (### 3): 유지 — 믹스인 레퍼런스로 필수

**섹션 5: 접근성 가이드 참조 경로 — 유지 + 프로젝트 고유 접근성 규칙 추가**
기존 경로 리스트 유지. 섹션 6에서 이동한 skip-to-content, sr-only 규칙 2줄 추가.

**섹션 6: 문서 사이트, GSD Workflow, Developer Profile — 유지**
변경 없음.

---

**최종 구조 (예상 ~250줄):**
1. Project (~18줄) — 그대로
2. Technology Stack (~20줄) — 축약
3. Conventions:
   - BEM 네이밍 규칙 (~30줄) — 코드 예시 제거
   - 디자인 토큰 사용법 (~25줄) — 값 나열 제거, 파일 참조
   - 린트 규칙 (~15줄) — 그대로 + SCSS 세미콜론 1줄
   - 컴포넌트 스니펫 (~15줄) — 그대로
4. Architecture (~130줄) — 소폭 축약
5. 접근성 가이드 참조 + 프로젝트 고유 규칙 (~10줄)
6. 문서 사이트 + GSD + Profile (~20줄)
  </action>
  <verify>
    <automated>LINES=$(wc -l < CLAUDE.md) && echo "Lines: $LINES" && test "$LINES" -le 280 && echo "PASS: under 280 lines" || echo "FAIL: over 280 lines"</automated>
    <automated>grep -c "GSD:" CLAUDE.md | xargs -I{} test {} -ge 12 && echo "PASS: all GSD markers present" || echo "FAIL: missing GSD markers"</automated>
    <automated>grep -q "src/scss/1-settings/_tokens-color.scss" CLAUDE.md && echo "PASS: token file ref exists" || echo "FAIL: missing token file ref"</automated>
    <automated>grep -q "하드코딩.*금지\|금지.*하드코딩" CLAUDE.md && echo "PASS: hardcoding ban rule exists" || echo "FAIL: missing hardcoding ban"</automated>
    <automated>grep -q "BEM" CLAUDE.md && echo "PASS: BEM rules exist" || echo "FAIL: missing BEM rules"</automated>
    <automated>grep -q "ITCSS" CLAUDE.md && echo "PASS: ITCSS rules exist" || echo "FAIL: missing ITCSS rules"</automated>
    <automated>! grep -q "var(--color-gray-900).*#222\|// #222" CLAUDE.md && echo "PASS: token values removed" || echo "FAIL: token values still present"</automated>
  </verify>
  <done>
    - CLAUDE.md가 280줄 이하 (기존 557줄 대비 50% 이상 감소)
    - GSD 마커 6쌍 (12개) 모두 보존
    - 토큰 값 나열이 파일 경로 참조로 대체
    - BEM 필수 패턴/금지 패턴 테이블, 하드코딩 금지 규칙, ITCSS 레이어별 BEM 적용 범위 테이블 유지
    - 글로벌 CLAUDE.md와 중복되는 CSS 규칙/코딩 스타일/접근성 기본 항목 제거
    - 하드코딩 vs 토큰 비교 예시 유지
    - 반응형 믹스인/공용 믹스인 레퍼런스 유지
  </done>
</task>

<task type="auto">
  <name>Task 2: 경량화 검증 — 규칙 보존 + 토큰 참조 확인</name>
  <files>CLAUDE.md</files>
  <action>
경량화된 CLAUDE.md를 Read하여 다음을 검증한다:

1. **GSD 마커 완전성**: 6쌍 12개 마커가 모두 존재하고, start/end가 올바르게 짝을 이루는지 확인
2. **판단 기준 보존 체크리스트:**
   - BEM 필수 패턴 테이블 (Block, Block+Modifier, Block+Element, Block+Element+Modifier)
   - BEM 금지 패턴 테이블 (.btn-primary, .card-header 등 6개)
   - SCSS 작성 규칙 4개 (&__element, &--modifier, 2단계 중첩 금지, Modifier 부착 규칙)
   - 하드코딩 vs 토큰 비교 예시
   - ITCSS 레이어별 BEM 적용 범위 테이블
   - 반응형 믹스인 사용법 (respond-to)
   - 공용 믹스인 레퍼런스 (flex-center, ellipsis 등)
   - 컴포넌트 스니펫 테이블
3. **제거 확인:**
   - 토큰 값(#222, #333, 3.2rem, 0.4rem 등)이 없는지 확인 — 하드코딩 vs 토큰 비교 예시 내 값은 예외
   - "코딩 스타일" 독립 섹션이 없는지 확인 (글로벌 CLAUDE.md로 위임됨)
   - "Alternatives Considered" 테이블이 없는지 확인
4. **라인 수 최종 확인**: 280줄 이하

문제 발견 시 CLAUDE.md를 수정한다. 문제 없으면 그대로 둔다.
  </action>
  <verify>
    <automated>LINES=$(wc -l < CLAUDE.md) && ORIG=557 && REDUCTION=$(( (ORIG - LINES) * 100 / ORIG )) && echo "Lines: $LINES (${REDUCTION}% reduction)" && test "$REDUCTION" -ge 50 && echo "PASS" || echo "FAIL"</automated>
    <automated>for marker in "GSD:project-start" "GSD:project-end" "GSD:stack-start" "GSD:stack-end" "GSD:conventions-start" "GSD:conventions-end" "GSD:architecture-start" "GSD:architecture-end" "GSD:workflow-start" "GSD:workflow-end" "GSD:profile-start" "GSD:profile-end"; do grep -q "$marker" CLAUDE.md || echo "MISSING: $marker"; done && echo "All markers checked"</automated>
  </verify>
  <done>
    - 경량화된 CLAUDE.md가 모든 판단 기준을 보존하면서 280줄 이하
    - GSD 마커 12개 모두 정확히 보존
    - 토큰 값 나열 없음 (파일 참조로 대체 확인)
    - 글로벌 CLAUDE.md 중복 항목 없음
  </done>
</task>

</tasks>

<verification>
1. `wc -l CLAUDE.md` — 280줄 이하
2. GSD 마커 12개 모두 존재 (grep 확인)
3. BEM/ITCSS/토큰 사용 규칙이 grep으로 검색 가능
4. 토큰 구체적 값(#222, 3.2rem 등)이 본문에 없음 (비교 예시 제외)
5. "코딩 스타일", "Alternatives Considered" 섹션이 없음
</verification>

<success_criteria>
- CLAUDE.md 총 라인 수 280줄 이하 (557줄 대비 50% 이상 감소)
- BEM 네이밍 규칙, 하드코딩 금지, ITCSS 레이어 규칙, 반응형/믹스인 레퍼런스가 유지됨
- 토큰 값이 파일 경로 참조로 대체됨
- 글로벌-프로젝트 CLAUDE.md 간 중복이 제거됨
- GSD 마커 6쌍 모두 보존됨
</success_criteria>

<output>
After completion, create `.planning/phases/28-claude-md-slim/28-01-SUMMARY.md`
</output>
