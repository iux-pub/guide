---
phase: 05-documentation-site
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - .pa11yci.js
  - src/scss/docs.scss
autonomous: false
requirements:
  - DOCS-04
  - DOCS-06

must_haves:
  truths:
    - "pagefind 검색에서 토큰명, 컴포넌트명, 가이드 키워드를 찾을 수 있다"
    - "빌드 후 _site/pagefind/ 디렉토리에 인덱스 파일이 존재한다"
    - "pa11y-ci 실행 시 문서 사이트 주요 페이지가 WCAG AA를 통과한다"
    - "문서 사이트가 브라우저에서 정상적으로 렌더링된다"
  artifacts:
    - path: "_site/pagefind/pagefind-ui.js"
      provides: "pagefind 검색 UI"
    - path: ".pa11yci.js"
      provides: "문서 사이트 URL 포함 pa11y-ci 설정"
      contains: "_site"
  key_links:
    - from: "site/_includes/partials/search.njk"
      to: "_site/pagefind/pagefind-ui.js"
      via: "script src"
      pattern: "pagefind-ui"
    - from: ".pa11yci.js"
      to: "_site/*.html"
      via: "file:// URL"
      pattern: "file://.*_site"
---

<objective>
pagefind 검색이 실제로 동작하는지 확인하고, pa11y-ci로 문서 사이트 전체의 WCAG AA 접근성을 검증한다. 검색 인덱스 생성 확인, 접근성 위반 수정, 최종 품질 확인을 수행한다.

Purpose: 문서 사이트의 검색 기능(DOCS-04)과 접근성 준수(DOCS-06)를 확인하여 Phase 5 완성도를 보장한다.
Output: pagefind 검색 동작 확인 + pa11y-ci 접근성 검증 통과
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-site/05-CONTEXT.md
@.planning/phases/05-documentation-site/05-01-SUMMARY.md
@.planning/phases/05-documentation-site/05-02-SUMMARY.md
@.planning/phases/05-documentation-site/05-03-SUMMARY.md

@.pa11yci.js
@eleventy.config.js
@src/scss/docs.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: pagefind 인덱스 생성 확인 + pa11y-ci 문서 사이트 URL 추가 + 접근성 검증</name>
  <files>
    .pa11yci.js,
    src/scss/docs.scss
  </files>
  <read_first>
    .pa11yci.js,
    eleventy.config.js,
    src/scss/docs.scss,
    site/_includes/layouts/base.njk
  </read_first>
  <action>
    1. `npm run build:site` 실행하여 전체 빌드 (Eleventy + pagefind 인덱싱):
       - eleventy.after 이벤트에서 pagefind가 자동 실행됨
       - _site/pagefind/ 디렉토리에 인덱스 파일 생성 확인
       - `ls _site/pagefind/` 로 pagefind-ui.js, pagefind-ui.css, *.pf 인덱스 파일 존재 확인

    2. .pa11yci.js에 문서 사이트 주요 페이지 URL 추가:
       - 기존 playground URL들 유지
       - 문서 사이트 URL 추가 (file:// 절대 경로):
         - _site/index.html (랜딩)
         - _site/tokens/index.html
         - _site/tokens/color/index.html (대표 토큰 페이지)
         - _site/conventions/index.html
         - _site/conventions/bem/index.html (대표 컨벤션 페이지)
         - _site/components/index.html
         - _site/components/btn/index.html (대표 컴포넌트 페이지)
         - _site/accessibility/index.html
         - _site/accessibility/checklist/index.html (대표 접근성 페이지)
       - path.resolve로 절대 경로 생성 (기존 .pa11yci.js 패턴 따름)

    3. pa11y-ci 실행: `npm run build:css && npm run build:docs-css && npx @11ty/eleventy && pa11y-ci --config .pa11yci.js`
       - 접근성 위반 발견 시 수정:
         - skip-to-content 링크 확인
         - nav role="navigation" aria-label 확인
         - iframe title 속성 확인
         - 색상 대비 4.5:1 확인
         - 키보드 네비게이션 확인
       - docs.scss 수정이 필요하면 수정 (예: 포커스 스타일, 대비 개선)

    4. pagefind 검색 기능 동작 확인:
       - `_site/pagefind/pagefind-entry.json` 또는 유사 인덱스 파일 존재 확인
       - 인덱싱된 페이지 수 확인 (pagefind 빌드 출력에서)
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run build:site 2>&1 | tail -10 && test -d _site/pagefind && echo "pagefind OK" && pa11y-ci --config .pa11yci.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - _site/pagefind/ 디렉토리에 pagefind-ui.js 파일 존재
    - _site/pagefind/ 디렉토리에 .pf 인덱스 파일 존재
    - .pa11yci.js에 `_site/index.html` 경로 포함
    - .pa11yci.js에 `_site/tokens/` 경로 포함
    - pa11y-ci 실행 시 문서 사이트 페이지에서 에러 0건 (warning은 허용)
  </acceptance_criteria>
  <done>pagefind 인덱스가 생성되어 검색 준비 완료, pa11y-ci로 문서 사이트 주요 페이지가 WCAG AA 통과</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 문서 사이트 전체 기능 확인</name>
  <files>없음 (검증만 수행)</files>
  <action>
    `npm run serve` 실행하여 개발 서버 시작 후 브라우저에서 문서 사이트 전체 기능을 확인한다.
    확인 항목:
    1. 랜딩 페이지에서 4개 섹션 링크 확인
    2. 좌측 사이드바에서 각 섹션 하위 페이지 네비게이션 확인
    3. 토큰 > 색상 페이지에서 색상 토큰 목록 확인
    4. 컨벤션 > BEM 페이지에서 BEM 가이드 내용 확인
    5. 컴포넌트 > 버튼 페이지에서 iframe 미리보기 + 코드 복사 동작
    6. 접근성 > 체크리스트 페이지에서 KWCAG 체크리스트 내용 확인
    7. 검색창에 "색상", "btn", "접근성" 등 입력하여 검색 결과 확인
    8. 브라우저 창 줄여서 모바일 반응형 확인
    9. Tab 키로 키보드 접근성 확인
  </action>
  <verify>사용자가 브라우저에서 직접 확인 후 "approved" 입력</verify>
  <done>사용자가 문서 사이트 전체 기능(네비게이션, 미리보기, 코드 복사, 검색, 반응형, 접근성)을 확인하고 승인</done>
</task>

</tasks>

<verification>
- `npm run build:site` 에러 없이 완료
- _site/pagefind/ 디렉토리에 검색 인덱스 존재
- pa11y-ci로 문서 사이트 주요 페이지 WCAG AA 통과
- 브라우저에서 전체 기능(네비게이션, 미리보기, 코드 복사, 검색, 반응형) 동작
</verification>

<success_criteria>
pagefind 검색이 동작하여 토큰명/컴포넌트명/키워드 검색 가능하고, pa11y-ci로 문서 사이트 접근성 검증이 통과하며, 사용자가 브라우저에서 전체 문서 사이트 기능을 확인하고 승인
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-site/05-04-SUMMARY.md`
</output>
