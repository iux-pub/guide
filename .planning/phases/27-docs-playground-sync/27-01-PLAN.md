---
phase: 27-docs-playground-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - src/snippets/btn.md
  - src/snippets/form.md
  - src/snippets/card.md
  - src/snippets/table.md
  - src/snippets/modal.md
  - src/snippets/tab.md
  - src/snippets/pagination.md
  - src/snippets/breadcrumb.md
  - prompts/components.md
  - prompts/publishing.md
  - prompts/context.md
  - prompts/review.md
autonomous: true
requirements: [TOKFIX-02]

must_haves:
  truths:
    - "src/snippets/ 마크다운이 실제 SCSS 구현과 일치한다"
    - "prompts/ 파일이 변경된 토큰/규칙/BEM 클래스를 반영한다"
    - "CLAUDE.md의 토큰 레퍼런스가 실제 값과 일치한다"
  artifacts:
    - path: "CLAUDE.md"
      provides: "--transition-fast 0.1s ease로 갱신"
      contains: "0.1s ease"
    - path: "prompts/components.md"
      provides: "탭/페이지네이션 BEM 클래스 수정"
      contains: "tab__button"
    - path: "prompts/publishing.md"
      provides: "transition-fast 값 갱신"
      contains: "fast(0.1s)"
    - path: "src/snippets/modal.md"
      provides: "모달 애니메이션/44px 반영"
      contains: "modal--active"
    - path: "src/snippets/card.md"
      provides: "카드 hover 효과 반영"
      contains: "hover"
  key_links:
    - from: "prompts/components.md"
      to: "src/scss/6-components/_tab.scss"
      via: "BEM 클래스명 일치"
      pattern: "tab__button"
    - from: "CLAUDE.md"
      to: "tokens.json"
      via: "토큰 값 정합"
      pattern: "0\\.1s ease"
---

<objective>
Phase 25-26에서 변경된 SCSS를 스니펫(src/snippets/), 프롬프트(prompts/), CLAUDE.md에 역갱신한다.

Purpose: 코드와 문서가 불일치하면 AI가 잘못된 코드를 생성하고, 신규 팀원이 혼란을 겪는다. 싱글 소스(SCSS)와 문서 간 100% 정합을 보장한다.
Output: 8개 스니펫 마크다운, 4개 프롬프트, CLAUDE.md 갱신
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-a11y-token-fix/25-01-SUMMARY.md
@.planning/phases/25-a11y-token-fix/25-02-SUMMARY.md
@.planning/phases/26-scss-modernize/26-01-SUMMARY.md
@.planning/phases/26-scss-modernize/26-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 스니펫 8개 파일 역갱신</name>
  <files>src/snippets/btn.md, src/snippets/form.md, src/snippets/card.md, src/snippets/table.md, src/snippets/modal.md, src/snippets/tab.md, src/snippets/pagination.md, src/snippets/breadcrumb.md</files>
  <read_first>src/scss/6-components/_btn.scss, src/scss/6-components/_form.scss, src/scss/6-components/_card.scss, src/scss/6-components/_table.scss, src/scss/6-components/_modal.scss, src/scss/6-components/_tab.scss, src/scss/6-components/_pagination.scss, src/scss/6-components/_breadcrumb.scss, src/snippets/btn.md, src/snippets/form.md, src/snippets/card.md, src/snippets/table.md, src/snippets/modal.md, src/snippets/tab.md, src/snippets/pagination.md, src/snippets/breadcrumb.md</read_first>
  <action>
각 스니펫 파일을 실제 SCSS와 비교하여 다음 변경사항을 반영한다. HTML 마크업은 변경 불필요 -- 접근성/CSS 동작 설명만 갱신.

**btn.md:**
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응: 모션 감소 설정 시 transition 비활성화"
- 접근성 주의사항에 추가: "반응형 패딩: 모바일/태블릿/PC에서 패딩이 차등 적용됨"

**form.md:**
- 접근성 주의사항 수정: "focus-visible 스타일 제공: outline: 2px solid + outline-offset: 2px (box-shadow가 아닌 outline 사용 -- 고대비 모드 호환)" 추가
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"
- 접근성 주의사항에 추가: "반응형 패딩: 모바일/태블릿/PC 차등 적용"
- Variant 목록 테이블에 정보 추가: 레이블 폰트가 16px(--font-size-base)

**card.md:**
- 접근성 주의사항 뒤에 "## 인터랙션" 섹션 추가:
  - "hover 시 box-shadow가 shadow-lg로 확대되고 translateY(-0.2rem) 효과 적용"
  - "transition: box-shadow, transform var(--transition-base)"
  - "prefers-reduced-motion: reduce 시 transition 비활성화"
- 접근성 주의사항에 추가: "반응형 패딩: header/body/footer 영역이 모바일/태블릿/PC에서 차등 패딩"

**table.md:**
- 접근성 주의사항에 추가: "기본 폰트 16px(--font-size-base) 이상"
- 접근성 주의사항에 추가: "반응형 패딩: th/td가 모바일/태블릿/PC에서 차등 패딩"

**modal.md:**
- Variant 목록 테이블에 행 추가: "| 활성 상태 | `.modal--active` | JS가 열 때 추가. 열림 애니메이션 트리거 |"
- 접근성 주의사항 뒤에 "## 열림 애니메이션" 섹션 추가:
  - "JS에서 `modal--active` 클래스를 추가하면 `@keyframes modal-open`이 실행됨"
  - "scale(0.95) -> scale(1) + opacity 0 -> 1, 300ms ease"
  - "prefers-reduced-motion: reduce 설정 시 animation: none"
- 닫기 버튼 설명 갱신: "닫기 버튼 (`aria-label="닫기"`) -- 44x44px 터치 타겟 보장"
- 반응형 설명 추가: "모바일: 전체화면 / 태블릿: max-width 56rem, max-height 90vh / PC: max-height 85vh"

**tab.md:**
- 접근성 주의사항에 추가: "focus-visible 스타일: outline: 2px solid + outline-offset: 2px"
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"
- 구성 요소 테이블 Button 행 설명 갱신: "`role=\"tab\"`, 패딩 12px/20px (모바일), 반응형 확대"

**pagination.md:**
- Variant 테이블 Link 행 갱신: "`<a>` 페이지 링크 -- 최소 44x44px 터치 타겟"
- 접근성 주의사항에 추가: "링크 간 간격 8px 이상 (gap: --spacing-sm)"
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"

**breadcrumb.md:**
- 접근성 주의사항에 추가: "아이템 간 간격 8px 이상 (gap: --spacing-sm)"
- 접근성 주의사항에 추가: "prefers-reduced-motion 대응"
- 접근성 주의사항에 추가: "태블릿 이상에서 폰트 사이즈 16px(--font-size-base)로 상향"
  </action>
  <verify>
    <automated>grep -c "prefers-reduced-motion" src/snippets/btn.md src/snippets/form.md src/snippets/card.md src/snippets/modal.md src/snippets/tab.md src/snippets/pagination.md src/snippets/breadcrumb.md | grep -v ":0$" | wc -l | grep -q "7" && grep -q "modal--active" src/snippets/modal.md && grep -q "44x44px" src/snippets/modal.md && grep -q "hover" src/snippets/card.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "prefers-reduced-motion" src/snippets/*.md -- 7개 파일 모두 1회 이상 매칭
    - grep "modal--active" src/snippets/modal.md -- 매칭
    - grep "44x44px" src/snippets/modal.md -- 매칭
    - grep "hover" src/snippets/card.md -- 매칭
    - grep "outline-offset" src/snippets/form.md -- 매칭
    - grep "outline-offset" src/snippets/tab.md -- 매칭
    - grep "44x44px" src/snippets/pagination.md -- 매칭
  </acceptance_criteria>
  <done>8개 스니펫 파일이 Phase 25-26 SCSS 변경사항(reduced-motion, focus-visible, 반응형 패딩, 터치 타겟, 애니메이션, hover 효과)을 빠짐없이 반영한다</done>
</task>

<task type="auto">
  <name>Task 2: 프롬프트 4개 + CLAUDE.md 토큰/규칙 갱신</name>
  <files>prompts/components.md, prompts/publishing.md, prompts/context.md, prompts/review.md, CLAUDE.md</files>
  <read_first>prompts/components.md, prompts/publishing.md, prompts/context.md, prompts/review.md, CLAUDE.md</read_first>
  <action>
**prompts/components.md -- BEM 클래스 수정 + 신규 패턴:**
- 탭 섹션: `tab__item` -> `tab__button` (실제 SCSS는 tab__button)
  - 기존: `<button class="tab__item" role="tab"` -> `<button class="tab__button" role="tab"`
  - 기존: `<div class="tab" role="tablist"` -> `<div class="tab">` + `<div class="tab__list" role="tablist">`로 분리 (실제 구조는 tab > tab__list[tablist] > tab__button[tab])
- 페이지네이션 섹션: 실제 BEM 구조로 교체
  - 기존: `pagination__prev`, `pagination__next`, `pagination__link--active` -> `pagination__list > pagination__item > pagination__link`, `pagination__link--prev`, `pagination__link--next`, `pagination__link--current`
  - `<nav>` 안에 `<ul class="pagination__list">` > `<li class="pagination__item">` > `<a class="pagination__link">` 구조
- 모달 섹션 끝에 추가: "JS: `modal--active` 클래스 토글로 열림 애니메이션 제어"

**prompts/publishing.md -- 토큰 값 수정:**
- "전환: `var(--transition-fast)`" 행은 이미 맞음 (값 표기 없음)
- 찾기: `fast(0.15s)/base(0.3s)/slow(0.5s)` -> 없으면 무시
- 접근성 섹션에 추가: "prefers-reduced-motion 대응: 모든 컴포넌트 transition/animation 비활성화"
- 접근성 섹션에 추가: "터치 타겟: 최소 44x44px, 인접 요소 간격 8px 이상"

**prompts/context.md -- 토큰 참조 갱신:**
- 기존: `--transition-fast` 토큰 참조 -- 값 표기가 없으므로 변경 불필요
- 내용이 이미 정확한지 확인 후, 변경 필요 시에만 수정

**prompts/review.md -- 체크리스트 추가:**
- 접근성 섹션에 추가: `- [ ] prefers-reduced-motion 미디어 쿼리가 있는가`
- 반응형 섹션의 기존 항목 유지, "터치 타겟이 최소 44x44px인가"는 이미 있으므로 확인만

**CLAUDE.md -- 토큰 레퍼런스 수정:**
- `var(--transition-fast)         // 0.15s ease` -> `var(--transition-fast)         // 0.1s ease`
  </action>
  <verify>
    <automated>grep -q "tab__button" prompts/components.md && grep -q "pagination__list" prompts/components.md && grep -q "0.1s ease" CLAUDE.md && grep -q "prefers-reduced-motion" prompts/review.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "tab__button" prompts/components.md -- 매칭
    - grep "tab__item" prompts/components.md -- 매칭 없음 (제거됨)
    - grep "pagination__list" prompts/components.md -- 매칭
    - grep "pagination__prev" prompts/components.md -- 매칭 없음 (제거됨)
    - grep "0.1s ease" CLAUDE.md -- 매칭
    - grep "0.15s ease" CLAUDE.md -- 매칭 없음
    - grep "prefers-reduced-motion" prompts/review.md -- 매칭
    - grep "modal--active" prompts/components.md -- 매칭
  </acceptance_criteria>
  <done>프롬프트 파일의 BEM 클래스가 실제 SCSS와 100% 일치하고, CLAUDE.md 토큰 값이 정합되며, 리뷰 체크리스트에 reduced-motion 항목이 포함된다</done>
</task>

</tasks>

<verification>
- grep "0.15s" CLAUDE.md prompts/ src/snippets/ -- 매칭 없음 확인
- grep "tab__item" prompts/components.md -- 매칭 없음 확인
- grep "pagination__prev" prompts/components.md -- 매칭 없음 확인
- 모든 8개 스니펫에 prefers-reduced-motion 언급 확인
</verification>

<success_criteria>
1. src/snippets/ 8개 파일이 실제 SCSS와 일치 (접근성, 인터랙션, 반응형 정보 반영)
2. prompts/components.md의 BEM 클래스가 실제 SCSS 클래스명과 100% 일치
3. CLAUDE.md의 --transition-fast 값이 0.1s ease
4. prompts/review.md에 prefers-reduced-motion 체크 항목 존재
</success_criteria>

<output>
After completion, create `.planning/phases/27-docs-playground-sync/27-01-SUMMARY.md`
</output>
