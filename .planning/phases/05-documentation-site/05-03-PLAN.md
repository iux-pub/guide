---
phase: 05-documentation-site
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - site/_includes/partials/component-preview.njk
  - site/components/components.json
  - site/components/index.md
  - site/components/btn.md
  - site/components/form.md
  - site/components/card.md
  - site/components/table.md
  - site/components/modal.md
  - site/components/tab.md
  - site/components/pagination.md
  - site/components/breadcrumb.md
  - site/components/boilerplate.md
  - site/assets/js/clipboard-init.js
  - site/_includes/layouts/base.njk
  - src/scss/docs.scss
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "빌드 후 _site/components/ 디렉토리에 10개 HTML 파일 존재"
    - "컴포넌트 페이지에 iframe으로 playground 미리보기가 표시된다"
    - "코드 블록에 복사 버튼이 우측 상단에 배치된다"
    - "복사 버튼 클릭 시 코드가 클립보드에 복사되고 '복사됨' 피드백 표시"
  artifacts:
    - path: "site/assets/js/clipboard-init.js"
      provides: "clipboard.js 초기화 + 복사 버튼 자동 추가"
      contains: "ClipboardJS"
    - path: "site/_includes/partials/component-preview.njk"
      provides: "iframe 미리보기 매크로"
      contains: "iframe"
    - path: "site/components/btn.md"
      provides: "버튼 컴포넌트 문서 페이지"
      contains: "playground_src"
  key_links:
    - from: "site/components/*.md"
      to: "site/_includes/layouts/component.njk"
      via: "front matter layout"
      pattern: "layout.*layouts/component"
    - from: "site/_includes/layouts/component.njk"
      to: "site/_includes/partials/component-preview.njk"
      via: "include"
      pattern: "include.*component-preview"
    - from: "site/assets/js/clipboard-init.js"
      to: "clipboard (npm)"
      via: "ClipboardJS 생성자"
      pattern: "ClipboardJS"
---

<objective>
8개 컴포넌트 + 보일러플레이트 문서 페이지를 생성하고, iframe 미리보기와 코드 복사(clipboard.js) 기능을 구현한다. Phase 3 playground HTML을 iframe으로 임베드하고, 스니펫 마크다운의 코드를 Prism.js로 하이라이팅하여 복사 가능하게 한다.

Purpose: 개발자가 컴포넌트를 시각적으로 확인하고 코드를 바로 복사하여 사용할 수 있게 한다 (DOCS-02, DOCS-03).
Output: 10개 컴포넌트 문서 페이지 + iframe 미리보기 + 코드 복사 기능
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-site/05-CONTEXT.md
@.planning/phases/05-documentation-site/05-01-SUMMARY.md

@src/playground/btn.html
@src/snippets/btn.md
@src/snippets/form.md
@src/snippets/card.md
@src/snippets/table.md
@src/snippets/modal.md
@src/snippets/tab.md
@src/snippets/pagination.md
@src/snippets/breadcrumb.md
@src/snippets/boilerplate.md
@site/_includes/layouts/component.njk
</context>

<tasks>

<task type="auto">
  <name>Task 1: clipboard.js 초기화 + 컴포넌트 미리보기 매크로 + 디렉토리 설정</name>
  <files>
    site/assets/js/clipboard-init.js,
    site/_includes/partials/component-preview.njk,
    site/components/components.json
  </files>
  <read_first>
    site/_includes/layouts/component.njk (Plan 01에서 생성),
    site/_includes/layouts/base.njk (Plan 01에서 생성),
    .planning/phases/05-documentation-site/05-RESEARCH.md (Pattern 4: iframe 미리보기, Pattern 5: clipboard.js)
  </read_first>
  <action>
    1. site/assets/js/clipboard-init.js 생성 (per D-04, per RESEARCH Pattern 5):
       - 바닐라 JS IIFE 패턴 (세미콜론 시작, 'use strict')
       - `document.querySelectorAll('pre[class*="language-"]')` 순회
       - 각 pre에 `.copy-btn` 버튼 추가: textContent='복사', aria-label='코드 복사' (per D-04 우측 상단)
       - `new ClipboardJS('.copy-btn', { target: trigger => trigger.closest('pre').querySelector('code') })`
       - success 콜백: '복사됨' 텍스트 변경 + aria-label='코드 복사됨' + 2초 후 원복
       - clipboard.js는 CDN 대신 node_modules에서 passthrough copy하거나, 또는 `<script src="https://cdn.jsdelivr.net/npm/clipboard@2.0.11/dist/clipboard.min.js"></script>`를 base.njk에 추가

    2. clipboard.js 로딩 방식 결정:
       - eleventy.config.js에 passthrough copy 추가: `{ 'node_modules/clipboard/dist/clipboard.min.js': 'assets/js/clipboard.min.js' }`
       - base.njk에 `<script src="/assets/js/clipboard.min.js"></script>` 추가 (clipboard-init.js 앞에)

    3. site/_includes/partials/component-preview.njk 생성 (per D-03, per RESEARCH Pattern 4):
       ```html
       {# 컴포넌트 미리보기 iframe 매크로 #}
       {% macro preview(src, title, height) %}
       <div class="preview">
         <h2 class="preview__title">미리보기</h2>
         <div class="preview__frame">
           <iframe
             src="{{ src }}"
             title="{{ title }} 컴포넌트 미리보기"
             height="{{ height | default(400) }}"
             loading="lazy"
             class="preview__iframe"
           ></iframe>
         </div>
       </div>
       {% endmacro %}
       ```

    4. site/components/components.json:
       `{ "layout": "layouts/component.njk", "section": "components", "tags": "component" }`

    5. src/scss/docs.scss에 preview 블록 스타일 추가 (docs.scss 파일 수정):
       - `.preview`, `.preview__title`, `.preview__frame`, `.preview__iframe` BEM 스타일
       - iframe: width 100%, border: 1px solid var(--color-border), border-radius: var(--radius-base)
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && test -f site/assets/js/clipboard-init.js && grep -q "ClipboardJS" site/assets/js/clipboard-init.js && test -f site/_includes/partials/component-preview.njk && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - site/assets/js/clipboard-init.js에 `ClipboardJS` 포함
    - site/assets/js/clipboard-init.js에 `복사됨` 피드백 텍스트 포함
    - site/_includes/partials/component-preview.njk에 `iframe` 태그 포함
    - site/_includes/partials/component-preview.njk에 `title` 속성 포함 (접근성)
    - site/components/components.json에 `layouts/component.njk` 포함
  </acceptance_criteria>
  <done>clipboard.js 초기화 스크립트와 iframe 미리보기 매크로가 생성되어 컴포넌트 페이지에서 사용 준비 완료</done>
</task>

<task type="auto">
  <name>Task 2: 컴포넌트 9개 + 보일러플레이트 문서 페이지 생성</name>
  <files>
    site/components/index.md,
    site/components/btn.md,
    site/components/form.md,
    site/components/card.md,
    site/components/table.md,
    site/components/modal.md,
    site/components/tab.md,
    site/components/pagination.md,
    site/components/breadcrumb.md,
    site/components/boilerplate.md
  </files>
  <read_first>
    src/snippets/btn.md,
    src/snippets/form.md,
    src/snippets/card.md,
    src/snippets/table.md,
    src/snippets/modal.md,
    src/snippets/tab.md,
    src/snippets/pagination.md,
    src/snippets/breadcrumb.md,
    src/snippets/boilerplate.md,
    site/_includes/partials/component-preview.njk (Task 1에서 생성),
    site/_includes/layouts/component.njk (Plan 01에서 생성)
  </read_first>
  <action>
    1. site/components/index.md — 컴포넌트 개요:
       - front matter: title="컴포넌트", order=0, layout=layouts/page.njk (개요는 page 레이아웃)
       - 8개 컴포넌트 목록 + 보일러플레이트 소개
       - 각 컴포넌트 링크

    2. 8개 컴포넌트 페이지 (btn.md ~ breadcrumb.md):
       각 파일의 front matter:
       ```yaml
       ---
       title: "{컴포넌트명}"
       order: {순서}
       playground_src: "/playground/{component}.html"
       playground_height: {적절한 높이}
       ---
       ```

       콘텐츠 구성:
       - component.njk 레이아웃이 자동으로 iframe 미리보기를 렌더링 (playground_src 참조)
       - src/snippets/{component}.md의 코드 예제를 마크다운 코드 블록으로 포함
       - HTML 스니펫: ```html 코드 블록
       - SCSS 스니펫: ```scss 코드 블록 (해당 시)
       - 사용법 설명, modifier 변형 안내

       playground 높이 가이드 (컴포넌트별 적절한 값):
       - btn: 500, form: 800, card: 600, table: 500
       - modal: 400, tab: 500, pagination: 300, breadcrumb: 200

    3. boilerplate.md — HTML 페이지 보일러플레이트:
       - front matter: title="보일러플레이트", order=9, layout=layouts/page.njk (미리보기 없으므로 page 레이아웃)
       - src/snippets/boilerplate.md의 HTML 코드를 코드 블록으로 포함
       - 사용법 설명: 새 프로젝트 시작 시 이 템플릿 복사

    **중요:** src/snippets/ 원본 파일은 수정하지 않는다. 코드 예제는 스니펫 파일에서 읽어와서 마크다운 코드 블록으로 재구성한다.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/Documents/Work/프로젝트/infoUX && npm run build:site 2>&1 | tail -3 && ls _site/components/ | wc -l && grep -q "playground" _site/components/btn/index.html 2>/dev/null && echo "iframe OK"</automated>
  </verify>
  <acceptance_criteria>
    - _site/components/ 디렉토리에 10개 하위 경로 존재 (index, btn, form, card, table, modal, tab, pagination, breadcrumb, boilerplate)
    - _site/components/btn/index.html에 iframe 또는 playground 참조 포함
    - site/components/btn.md에 `playground_src: "/playground/btn.html"` front matter 포함
    - 코드 블록에 `language-html` 또는 `language-scss` 클래스가 Prism.js에 의해 적용됨
  </acceptance_criteria>
  <done>8개 컴포넌트 + 보일러플레이트 문서 페이지가 빌드되어 iframe 미리보기와 Prism.js 코드 하이라이팅이 적용된 코드 예제가 표시됨</done>
</task>

</tasks>

<verification>
- `npm run build:site` 성공
- _site/components/에 10개 페이지 HTML 존재
- 컴포넌트 페이지에 iframe (playground src) 포함
- 코드 블록에 Prism.js 하이라이팅 클래스 적용
- clipboard-init.js에 ClipboardJS 초기화 코드 존재
- 복사 버튼이 코드 블록에 추가됨 (브라우저에서 확인)
</verification>

<success_criteria>
8개 컴포넌트 페이지에서 Phase 3 playground를 iframe으로 미리보기하고, Prism.js로 하이라이팅된 코드 예제를 clipboard.js로 복사할 수 있으며, 보일러플레이트 페이지에서 HTML 템플릿 코드를 확인 가능
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-site/05-03-SUMMARY.md`
</output>
